# Tasks

The shared ledger. Every collaborator — human or Claude — reads this before
starting work and updates it as work moves. It lives on `main` so that all
clones see the same picture.

`ROADMAP.md` says *what the project is trying to become* and why, in prose.
This file says *who is doing what right now*. When they disagree, this one is
current and the roadmap is the intent.

**Working on this file:** see `.claude/skills/ledger/SKILL.md` for the protocol
— how to claim a task without colliding with another session, and what counts
as verified. The short version: `git pull --rebase` first, change only the rows
you own, push immediately.

---

## Status vocabulary

| Status | Means |
|---|---|
| `open` | Described well enough to start. Nobody is on it. |
| `claimed` | Somebody is working on it now. Owner and branch are filled in. |
| `blocked` | Cannot proceed. The reason is in the notes, naming what would unblock it. |
| `done` | Written, tested, pushed. **Not yet independently checked.** |
| `verified` | Somebody other than the author ran it and confirmed the claim. |

`done` and `verified` are deliberately different columns. This project's
standard is that a claim is only closed by evidence, not by an assertion that
the work happened — the same reason `ROADMAP.md` records where Wave 0's
estimates were wrong. **Anything in `done` is unverified until someone else
says otherwise.**

The `Evidence` column is what makes `verified` mean anything: name the test, the
command, or the measurement. "Looks right" is not evidence.

---

## Active

Newest at the bottom of each section. Never renumber an ID — other commits,
branches and notes refer to them.

| ID | Task | Wave | Status | Owner | Branch | Evidence |
|---|---|---|---|---|---|---|
| T001 | Labels from `display_label`/unit, not raw column names | 1 | done | Claude (Fenil) | `feat/wave-1-definition-of-done` | `tests/test_variable_labels.py` — 15 tests; mutation-checked (removing the plumbing kills 6) |
| T002 | Load Inter, Source Serif 4, JetBrains Mono | 1 | done | Claude (Fenil) | `feat/wave-1-definition-of-done` | `apps/web/tests/fonts.test.ts` — 7 tests walking token → `@font-face` → woff2 bytes on disk |
| T003 | Pre-loaded worked example replacing the upload-prompt empty state | 1 | done | Claude (Fenil) | `feat/wave-1-worked-example` | `tests/test_worked_example.py` — 6 tests. The example is a **real project**: two sources ingested through the real pipeline, discovery running real sandboxed analyses, a finding with `association_only`. Asserted on provenance (every `analysis_run` completed with a p-value) rather than on the screen. Idempotent per user, and deletable like any other project. ~~Caveat: the `FirstProject` button has no test.~~ **Closed by Claude (Sarthak):** the component was module-private inside `page.tsx`, so the one control between a new account and a working project was the one control no test could import. Now exported, with `apps/web/tests/firstproject.test.tsx` — 5 tests covering the POST, the reload, the §104 error message and that the button re-enables after a failure so the researcher can retry. |
| T004 | Data-table alternative per chart, and `prefers-contrast` | 1 | done | Claude (Fenil) | `feat/wave-1-definition-of-done` | `apps/web/tests/chart-tables.test.tsx` — 7 tests; all 13 primitives render a `ChartTable`; `prefers-contrast: more` and `forced-colors: active` blocks in `globals.css`. ~~Contrast blocks are untested.~~ **Closed by Claude (Sarthak):** `apps/web/tests/contrast.test.ts` — 5 tests. happy-dom still applies no CSS, so this walks the chain instead, the way `fonts.test.ts` does: every class the `forced-colors` block overrides must be one the primitives really render (they live in `components/charts/`), no literal colour may survive into that block, and every token `prefers-contrast` remaps must be defined elsewhere in the stylesheet. Mutation-checked: drifting a chart class fails 1, a hex in forced-colors fails 1, a typo'd token fails 1. **The token check passed with a typo'd token on the first attempt** — it searched the whole stylesheet, which includes the block under test, so every remap defined itself; it now searches outside the block. **Still unproven: whether the contrast is actually sufficient for a reader who needs it.** That needs a real browser in that mode. |
| T005a | Measure the 60fps-at-5,000-nodes claim | 1 | done | Claude (Fenil) | `feat/wave-1-definition-of-done` | `apps/web/tests/graph-performance.test.ts`. **The claim was false and the comment is now corrected** — see D005. |
| T005b | Measure marketing LCP | 1 | **done** | Claude (Fenil) | `feat/wave-1-definition-of-done` | Measured on a production build, 5 runs each. **Unthrottled: LCP 560ms, CLS 0.000. Slow 4G + 4x CPU: LCP 1404ms, CLS 0.083.** Both well inside Google's "good" (LCP ≤2500ms). LCP element is `p.l-lede`, not the hero headline. Harness: `scripts/measure-lcp.mjs`. |
| T005c | Measure the five-click provenance depth | 1 | **done** | Claude (Sarthak) | `feat/wave-2-interpretation` | **Measured, and the answer is that the path does not exist.** Driven against the full stack — API, worker and a production web build on a scratch `THROUGHLINE_HOME`, with the T003 worked example run through the real pipeline (1 paper, 1 dataset, 6 analyses, 1 promoted finding). Overview → Findings → the finding is **2 clicks**, and there the trail stops: the finding detail's only affordance is *"Preview this as a library note"*. It links to no connection, no analysis run, no source. The sideways route (Connections → the connection) reaches the result in 2 clicks and also ends — its provenance line `⛓ 0 sources · 1 dataset` is plain text, not a link. So there is no click count to report, which is a more useful result than a number would have been. Recorded as D018; `ROADMAP.md` corrected. Harness: `scripts/measure-provenance.mjs`. |
| T007 | Read `artifact_renders.resolved_hash` back, so §102 is checkable rather than trusted | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `tests/test_artifact_staleness.py` — 18 tests, **mutation-checked**: removing the version check fails 3, the empty-hash guard 1, the superseded rule 3, the unresolvable guard 1, the `blocked` guard 1. `tests/test_interpretation_api.py` +3 including a cross-project document-id leak. `apps/web/tests/exports.test.tsx` — 11 tests. The hash was written on every render since the schema was laid down and compared by nothing, so the schema comment claiming a render is "provably stale" was false. ~~Caveat: verified only against synthetic render rows.~~ **Closed:** `tests/test_communication.py::test_a_real_render_is_current_against_the_analyses_it_read` and `::test_a_re_corrected_q_value_makes_a_real_export_drift` drive the real render path. The drift test also corrected a wrong assumption — a completed run **cannot** drift (`analysis_runs_immutable()` refuses to edit a terminal run), so the real §102 scenario is a **q-value re-corrected as the multiple-testing family grows**, which is reachable only through a connection reference. |
| T008 | Write the `contradictions` table, so the overview's Contradictions meter stops asserting a claim nothing supports (§55) | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `tests/test_contradictions.py` — 18 tests, **mutation-checked**: reverting `CHECK_ORDER` to the docstring's four fails 1, showing unexamined as ruled-out 1, reopening closed rows 1, dropping the required reason 1, defeating the dedupe lookup 4 (which also proves the new unique index is a real backstop). `tests/test_interpretation_api.py` +3 including a cross-project close-someone-else's-contradiction leak. `apps/web/tests/contradictions.test.tsx` — 12 tests. **`test_the_overview_count_moves_off_zero` asserts through `graphs.discovery_map`, the query the meter actually renders** — asserting on the table alone would have left the real defect unproven. **Caveat: only F7 is recorded.** A divergence the record already explains (F2/F3/F4/F6/F8/F10) is deliberately not written, so the meter counts unexplained disagreements only, not all of them. |
| T009 | Close the §137 loop in the browser — record a finding by clicking | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `PLAN.md` §137 item 5: the Overview said "Record a finding — NEXT" while the Findings section rendered zero buttons and **no `api.post` to `/findings` existed anywhere in `apps/web`**. The capability was in the API and exercised by the suite; it was unreachable by click, and the Findings empty state instructed the researcher to do the thing the interface did not offer. `components/recordfinding.tsx`, mounted on the **connection detail** rather than the Findings list, so the connection travels with the request as `from_connections` and the finding is attached to its analysis the moment it exists (D018's edge). `apps/web/tests/recordfinding.test.tsx` — 7 tests, mutation-checked: dropping the connection from the payload fails 1, dropping the causal disclaimer fails 1. **No control is offered to mark the result causal** — a dropdown offering it beside a correlation, just after a result survived, is the easiest place in the product to overclaim. The button is deliberately live before validation too, since the lifecycle already refuses an unearned promotion and a second enforcement here would only remove the researcher's judgement. |
| T010 | Health reports whether anything is draining the work queue | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Every existing check asks whether a *dependency* is reachable; none asked whether work is getting done, and in this product those differ — ingestion, discovery and analysis all run through the durable queue. With the database healthy and no worker running, the API answers every request correctly, `/api/health` reported ok, and nothing ever completed. Not hypothetical: `PLAN.md` recorded two rows stuck, and the worked example on this branch sat at zero findings during T005c until somebody noticed no worker had been started. `tests/test_observability.py` +7, mutation-checked: removing stall detection fails 1, treating silence as a confirmed worker fails 1, counting not-yet-due retries as waiting fails 1. **An idle queue is reported as neither a failure nor a working worker** — `ok` stays true so an idle workspace does not cry wolf, and `confirmed` is false so silence is never sold as health. Not critical: a stalled queue must not take the API out of rotation, because restarting the API fixes nothing and the worker is the process to start. |
| T011 | Every GET route answers, rather than raising | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `tests/test_every_route_answers.py` — 56 routes, discovered from the app rather than listed, so a new one is covered the moment it is added. Guards the *symptom* of D022 (a route that cannot answer) where `test_imports_resolve.py` guards the cause. **The first version was worthless and the mutation proved it:** it filled `{project_id}` with a made-up id, so `scoped_project()` returned 404 before any handler body ran — removing the `patterns` import failed nothing at all, because the test was exercising the authorisation guard fifty-six times. With a real project it fails on exactly the routes that broke: 2 for `patterns`, 3 for `notebook`. A 503 that names an unconfigured optional capability counts as answering — `/graph/centrality` and `/graph/communities` do that by design, and failing them would have been the test being wrong about correct code. |
| T012 | Export a figure at a stated size, in the format a journal asks for | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `publication.render` gains an exact `height_px` and four formats (tiff, jpeg, webp, eps beside svg/pdf/png); `GET /api/visuals/{id}/download` returns the file. `tests/test_visuals.py` +12, `tests/test_api.py` +3, mutation-checked: keeping the tight bbox fails the exact-height tests, dropping the size from the filename fails 2, ignoring a vector height fails 1. **Heights, not "1080p".** That name means a 16:9 video frame and a figure's aspect is set by its content (6.5x4.2in ≈ 1.55:1) — so the height is exact and the width follows, rather than letterboxing or stretching the axes. **Exact dimensions required turning off the tight bounding box**, which trims to content and makes the delivered size unpredictable; a constrained layout fits labels inside the figure instead of growing it. **JPEG is offered with the reason not to use it** — these are line art and text, where DCT rings around glyphs, there is no alpha, and PNG is usually smaller as well as exact; `warn_about_format` returns that before the download, and says nothing for a photograph, where JPEG is reasonable. Provenance is embedded in the file itself (visual id, spec hash), because a figure leaving the building is the one output whose link back cannot be a foreign key. |
| T013 | Reconcile the registered plan against the analysis actually run, and make the exemption earned | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `deviations.py` + `0031_registered_plan.sql`; `tests/test_deviations.py` — 14 tests, `tests/test_interpretation_api.py` +3. **Closes a loophole in this codebase's own design.** `exploration.record` granted the confirmatory exemption when a registration existed, was unedited and predated the test — all three true of an analysis with nothing to do with the plan. Register one comparison, run forty-seven variants, claim the winner: every check passed because nothing compared the *content*. A test that names its spec now keeps the exemption only if the analysis matches the plan. Mutation-checked: treating silence as intent fails 7, dropping canonical resolution 1, reopening the loophole 1, dropping the plan at the HTTP boundary 1. **Three decisions the usability rests on** — an unstated field is `unregistered`, not a deviation (otherwise every registration ever written, including every existing row, reports as violated); a harmonised rename is not a change (`ddd` and `antibiotic_consumption` are one quantity once approved, and reporting otherwise is the noisy failure that gets a checker switched off); and nothing calls a deviation misconduct, because deviating is usually right and a tool that says otherwise gets closed. ~~Caveat: `for_project` does not walk each registration's executed analyses.~~ **Closed:** `0032_test_provenance.sql` records the spec a look ran and the registration it *claimed*, so the reconciliation walks registration → tests → specs. That second column exists because a deviating test has its `preregistration_id` cleared — arithmetically right, and it was erasing the evidence that the analysis had been offered against the plan at all. `deviations.narrative` generates the *Deviations from the registered plan* section from the record, with every reason left blank: the system knows what changed, only the researcher knows why, and generating that would be inventing the one part of a methods section that has to be true. Mutation-checked: discarding the claim again fails 4, filling in the reason fails 1. **Caveat: still no UI.** |
| T014 | Spatial Interaction: the intent layer, the state machine and the safety rules | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | The layer the feature lives or dies on, built first because it is the only part that can be *evidenced* without a camera. `lib/spatial/{commands,filter,machine,types}.ts`; `apps/web/tests/spatial-machine.test.ts` — 21 tests over synthetic landmark streams. **Six guards mutation-checked**: hysteresis fails 1, dead zone 1, the pinch clutch 3, release-on-tracking-loss 1, the confidence gate 1, spike rejection 1. **No camera, no tracker, no 3D engine yet, and that is deliberate** — see D028. **Two of my own tests were worthless until the mutations exposed them.** Release-on-loss was covered by a test that returned the hand 0.57 away, so the *spike guard* rejected the movement and the outcome looked right while the gesture was still live underneath; and the spike test allowed any delta under 0.5, which the One Euro filter satisfies on its own. Both now isolate one mechanism each. |
| T015 | Spatial Interaction: the 3D chart driven through the controller seam | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Closes Prototype 1's other half. `Volume` (P13) now implements `VisualizationController` through an imperative handle: rotate, bounded zoom, nearest-point hover, selection that persists, focus, deselect, resetView, viewport. `apps/web/tests/volume-controller.test.tsx` — 15 tests driving the chart with the *same* `IntentCommand`s a pinch produces, so the whole chain is exercised with no camera. **Hover and click-select were added to the mouse path at the same time** — a capability reachable only by gesture would make the camera mandatory for part of the chart, which Rule 5 forbids. Mutation-checked: measuring click travel from the drag step fails 1, unbounded nearest-object fails 2, removing the zoom clamp fails 1. **A real bug found while writing it:** `dragRef` is reassigned on every pointer move, so measuring "did this click travel" against it always reported ~0 and *every* drag would have ended by selecting whatever the pointer stopped over; travel is now measured from where the pointer went down. **And one of my own tests was wrong rather than the code** — it asserted nothing may leave the viewport when zoomed in, but cropping is what zooming in *is*; it now asserts the scale is bounded, which is the property §32 actually needs. |
| T016 | Spatial Interaction: the camera, and the two failures that would end the feature's credibility | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `lib/spatial/{camera,tracker,session}.ts`; `apps/web/tests/spatial-session.test.ts` — 17 tests, the whole chain (permission → tracking → intent → chart) driven with no hardware via a mocked `getUserMedia` and a scripted tracker. **§12's architecture is the easy half** — frames never leave the browser because nothing on this path has a network call to send them with. The half that decides whether anybody trusts it is two mundane failures, and both are now asserted rather than intended: **a camera that stays on** (`stop()` stops *every* track, not the first; survives a track that throws; idempotent, because teardown runs from unmount, from failure and from the user's own control) and **a scene that moves after the light goes out** (`onFrame` drops any frame arriving when the session is not running — tracker callbacks are asynchronous and one can be in flight at switch-off). A failed tracker load releases the camera it had already opened, since a failed start that leaves the light lit is indistinguishable from the feature running. Mutation-checked, **8 for 8**: stopping only the first track, acting on frames after teardown, leaking the camera on a load failure, reopening a second stream, requesting audio, collapsing every refusal to one message, tearing the camera down on pause, and starting the tracker after a refusal — each caught. Telemetry is a bare tally of counts (`SpatialTelemetry`), deliberately not an event stream: a log of timestamps and confidences is a behavioural trace of somebody at their desk, and a feature cannot ask for a camera while quietly building one; totals still answer whether gestures are completed or abandoned. **The MediaPipe tracker is deliberately not imported** — Hand Landmarker fetches its WASM bundle and `.task` model from a CDN by default, and a silent network fetch on enabling a *privacy* feature is the wrong default for a product whose first claim is that nothing leaves the machine. The seam is defined and honoured; the implementation lands when the model is served from this installation. **Still open in Prototype 1:** calibration, the on-screen overlay, the settings panel and the feature flag. |
| T017 | Spatial Interaction: thresholds that mean the same thing for a hand that is not mine | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `lib/spatial/calibration.ts`; `apps/web/tests/spatial-calibration.test.ts` — 17 tests. **`pinchOn: 0.035` was an unstated assumption about one hand at one distance from one camera**, and it fails two people in opposite directions. Sitting back from a large display, a *resting* hand — fingers loosely curled, thumb near the index — is already inside the threshold, so the machine reads a grab nobody made and the scene follows idle movement (Rule 2, in one number). Leaning close to a laptop, a genuine pinch never gets small enough and the feature does nothing, with no explanation on screen. **Both look like bad tracking**, and neither would ever be reported as a threshold problem. The fix is that every hand carries its own ruler: wrist-to-index-base is a bone length, so it varies with distance from the camera and not with the pose, and thresholds expressed as a fraction of it mean the same thing at any size — no setup at all, which is why `adaptiveThresholds` defaults on. The ratios are the absolute defaults over the 0.10 span they were written against, so the two paths agree for a reference hand instead of quietly redefining a pinch. Explicit calibration (§17) is two poses, not a wizard, and **refuses rather than returning a number it cannot justify**: too few samples, poses that did not separate by a margin, or a hand that moved while it was measured — a threshold placed inside the noise leaves the researcher believing they are set up and blaming the tracking for the rest of the session. Samples are reduced with a median because one lost fingertip moves a mean past the separation being measured. Mutation-checked **12 of 13 caught**; the survivor was the finding: **a `span <= 0` guard that deleting changed nothing**, because a zero span already makes the ratio non-finite and the next line rejects it — defensive-looking dead code, now removed rather than covered by a test written to justify it. **Two of my own errors, both found by running it:** the test fixture placed wrist and index base 2x apart, so every ratio it measured was half what the test said, and two assertions passed anyway because halving both poses preserves their separation; and I wrote that a *wide open* hand falls inside the fixed threshold at distance — the arithmetic says it does not at any plausible span, and the true claim is the narrower one about a resting hand. Existing fixtures in `spatial-machine` and `spatial-session` were resized to `REFERENCE_SPAN`, since the absolute numbers quoted in their comments were never that hand's thresholds. |
| T018 | Spatial Interaction: what it costs to leave switched on | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §13 requires that the vision work never degrade the rendering it exists to serve, and that was the one claim in this feature nothing had measured. **Measured first: the intent layer is not the risk.** `SpatialInteractionMachine.step` costs ~1–2µs per frame and stays flat across 50,000 frames — 0.005% of a 33ms budget. Everything wired *around* it at frame rate is the risk, and three real costs were there. **(1) State published every frame.** `onState` fired on all ~30 frames a second even though a held grab reports `GRABBED` unchanged throughout; observers are React setters in practice, so this asked the workspace to re-render at tracker rate, continuously, for the entire time the feature was on. Now published only on change. **(2) No rate governor.** A tracker delivering at camera rate (60 Hz is common) would drive machine and chart at twice the rate the smoothing was tuned for, and since One Euro's cutoffs are expressed in time, that changes how much it smooths — not merely wasted work. Capped at ~33 Hz, and **deliberately not 1000/30**: a nominally-30 Hz tracker delivers ~33.3ms apart with jitter, so a floor at exactly 33.3 would reject every frame arriving a millisecond early and intermittently halve the rate of the stream it was sized for. Dropping is legitimate rather than lossy for a specific reason — every command is a *delta* from the last processed position, so a skipped frame makes the next delta larger instead of losing motion, which is asserted by comparing total rotation at 60 Hz against 30 Hz. **(3) Work while the tab is hidden.** `rAF` stops itself; a timer-driven tracker does not, so frames were processed to produce commands that repainted nothing. Now dropped — but the camera is deliberately **not** stopped, since someone who switched windows for ten seconds did not ask to be logged out, and re-requesting the stream can re-prompt. **A real bug found in the governor's own design:** frame timestamps belong to the tracker, and a fresh tracker's clock may restart near zero, so carrying `lastProcessed` across a restart would reject every new frame until the new clock passed the old one's final value — a session stopped after ten minutes would appear to start and then do nothing for ten minutes, camera light on, no error. `stop()` now forgets the clock. **`Volume`'s paint path had never been executed by any test** — `volume-controller` stubs `getContext` to null, so `draw()` returned on its second line and the depth sort, occlusion count and per-frame work were entirely uncovered. `apps/web/tests/volume-paint.test.tsx` supplies a recording 2D context instead: 5 tests covering that the cloud draws, that many commands still produce one paint, that an idle chart paints nothing, and the React-churn property. **Three of my own errors, each caught by measuring rather than reasoning.** I first 'fixed' the occlusion churn with a value-equality check — worthless, because on a dense cloud the count genuinely changes almost every frame (419, 422, 420, 425), which is also why React's own bail-out cannot help; the real fix is to stop asking at frame rate and publish once the view settles (120ms), which is both cheaper and more readable than a caption cycling through eight values a second. My first version of the test then **passed against the broken code** because it wrapped 60 frames in a single `act()`, so React batched away the very thing it measured — committed one frame at a time it showed 45 commits per 60 frames. And the fixture used 120 points, at which occlusion sat at a constant 2, so both the dedupe *and* the counting itself could be deleted with everything still green; it takes ~900 points before marks actually slide behind one another. A fourth: the settle test used fake timers, which also fake the scheduler React flushes state through — it passed alone and failed in the suite. It now uses real timers and asserts the caption text, because a commit count would still pass if the component re-rendered without ever saying anything. |
| T019 | Spatial Interaction: the part a researcher can actually reach | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Everything before this was an engine nothing in the product could reach. `lib/spatial/preferences.ts` + `components/spatial/SpatialControl.tsx`, wired to the P13 scatter in the gallery — the only genuinely 3D primitive, since §3 excludes enabling gesture for ordinary 2D charts. `apps/web/tests/spatial-preferences.test.ts` (17) and `apps/web/tests/spatial-control.test.tsx` (15). **The tests are almost entirely about ordering, because that is what trust is here.** No camera is requested on mount; opting in does not start one; the explanation is a step that precedes the browser's prompt (§4); `getUserMedia` is called only after a press on a button that says so; and 'Not now' is a real answer. Remembering that someone enabled the feature never resumes their camera on a later visit — enabled means *offered*, and starting is an explicit act every session. Unmounting releases the stream, because a component that goes away with a camera open leaves the light on and no control on screen to explain it. **Stored settings are treated as untrusted input**, since they come from disk, can be hand-edited, and are read during a render: `enabled` must be strictly `true` (a stored `"yes"` or `1` must not switch on a webcam feature), a blob from another version is discarded rather than migrated, malformed JSON returns defaults instead of failing a render, and every number is clamped — a stored `rotationSensitivity` of 1e9 would otherwise make the scene unrecoverable without clearing site data. Crossed pinch thresholds are repaired as a pair, because a release threshold below the engage threshold makes the gesture impossible and reads as broken tracking rather than a bad setting. A test asserts the stored blob contains no timings, confidences or history — §12 checked rather than claimed. **Two defects found by building it.** Attaching the stream to the `<video>` element was unguarded, so a failed assignment threw out of `start()` with the camera already open and no session to switch it off with — the camera-left-on failure by the one route that leaves nothing on screen to fix it; found because happy-dom type-checks `srcObject` exactly as a browser does, which also meant the first version of the test exercised the failure path while claiming to test the happy one. And the panel read **"Not running."** at the moment the camera light came on, because no state is published until a frame is processed — false, and the fastest way to convince someone the feature is broken. **I reintroduced D025's exact defect** — `className="linkish"`, a class no stylesheet defines — in the file whose whole subject is being careful. CSS fails silently, so nothing caught it. `apps/web/tests/css-classes.test.ts` now fails on any literal `className` with no rule, verified by reintroducing `linkish` and watching it name the file. Interpolated `className={...}` is deliberately skipped: a static reading would produce false positives, which is how a guard gets suppressed rather than fixed (D021). Thirteen pre-existing unstyled classes are in an explicit allowlist rather than fixed, because inventing styles for chart internals blind would change how those figures look with nothing to check against; a second test fails if an allowlisted class later gains a rule, so the list cannot quietly stop describing anything. **Still not wired: MediaPipe.** The control constructs a `ScriptedHandTracker`, so the camera turns on and no hand is tracked. That is the honest state — Hand Landmarker fetches its model from a CDN by default, and a silent network request on enabling a *privacy* feature contradicts the product's first claim. **Not verified in a browser:** CSS layout is unchecked, because the port the harness attaches to is held by another process. |
| T020 | The container could not be built on the machines most likely to build it | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Installed Docker (Colima, userspace — Docker Desktop needs admin rights) and ran CI's image job locally for the first time. **It failed, and CI cannot see why it would.** `pgserver` publishes no linux-aarch64 wheel in any release and no sdist, so on an ARM host `docker build` dies with `No matching distribution found for pgserver`. CI runs `ubuntu-latest`, which is amd64, so the job is green and would stay green forever while the documented container route was broken for the people most likely to take it — Docker on Apple Silicon requests linux/arm64 by default. The runtime stage is now pinned to `linux/amd64` (the web stage stays native: it emits JS and CSS, identical on any architecture), with `platform: linux/amd64` in compose so the mismatch cannot resurface at run time. `tests/test_packaging.py::test_the_runtime_stage_is_pinned_to_an_architecture_pgserver_supports` fails if the pin is removed. **The pin fixes the build and not the run**, which took a second round of digging to establish honestly. With it, the image builds on ARM and then PostgreSQL crashes during migration 0001. Bisected to the exact statement: plain DDL fine, `SELECT` fine, `CREATE EXTENSION vector` kills the backend — pgvector is compiled C using SIMD instructions and does not survive qemu's x86-64 emulation. So the container is **x86-64 only**, as a hard limit rather than a default, and Apple Silicon users must use `scripts/bootstrap.sh`. `throughline_domain/preflight.py` now checks the extension before migrations and, on failure, prints an explanation naming the cause, saying the volume is fine, and pointing at the native install — then exits. It uses `os._exit` on that path only, because pgserver's atexit handler otherwise spends a minute failing to stop a server that already crashed and buries the message under twenty lines of connection errors. The check and the migrations share one process, since each process that touches the database starts and stops its own cluster. `tests/test_preflight.py` — 8 tests. Documented in the README, explicitly noting that CI cannot catch it. **One correction to my own verification.** I first reported the container's API healthy. It was not: `curl 127.0.0.1:8080` was answered by the user's own API already running on the host, and the container had already exited. Re-run on a free port, the container genuinely failed — the health I reported was another process's. |
| T021 | Spatial Interaction: an actual hand | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | The gap that made everything before it an engine rather than a feature: the camera turned on and nothing was tracked. `lib/spatial/mediapipe.ts` + `scripts/vendor-hand-model.mjs`; `apps/web/tests/spatial-mediapipe.test.ts` — 15 tests. **The objection that held this up is resolved rather than waived.** MediaPipe's documented example passes a jsdelivr URL to `FilesetResolver`, so enabling the feature would fetch the runtime and a 7.5MB model over the network at the exact moment a researcher switches on something whose entire claim is that nothing leaves their machine — and it would break on a plane, on a locked-down university network, and the day the CDN path moves. Both halves are now vendored into `public/mediapipe/` at *install* time (the WASM copied from the pinned npm package, the model downloaded once and checked against a pinned SHA-256 that the script refuses to install without) and served by this application. Installing software fetches things; the promise is about what happens once it is running. **Verified end to end against the running server:** all four assets return 200 from the app itself, `application/wasm` on the wasm (browsers require it for streaming compilation), and the served model hashes to exactly the pinned digest. A test fails on any `http(s)://` literal in the tracker. **The landmark mapping is where the real risk was**, and it is the only part testable without a camera: every line is an array index, and a thumb read from the ring finger's slot still yields a number, a pinch distance, and a scene that rotates — just never when the researcher meant it. Fixtures place landmark `i` at `x = i/100`, so a mis-index names the slot it was actually read from. Pinned: all seven mapped indices, that a partial hand is dropped rather than yielding NaN rotations, that handedness is reported from the researcher's point of view rather than the camera's (MediaPipe inverts it), and that a missing confidence defaults to 1 rather than 0 — 0 would leave the confidence gate silently blocking everything, so the feature would appear dead with the camera on. **Palm centre is the wrist-and-knuckles centroid**, and the choice is asserted: a fingertip moves as the hand closes, so rotation measured from one would drift every time the researcher pinched; the wrist alone sits at the edge, so rotating it swings further than the hand travelled. A test closes the fingers and asserts the palm does not move. Inference is rAF-driven and rate-limited to ~33 Hz (rAF also stops when the tab is hidden, which is the right behaviour for a camera loop), the model is dynamically imported so a workspace whose owner never enables this does not carry it — **measured: the workspace bundle grew 1 kB, 106→107 kB** — and the tracker is closed on stop to free tens of megabytes of WASM heap. A load failure names the fix (`npm run vendor:hand-model`) rather than surfacing a 404 on a `.task` file. **Not verified: a real hand.** That needs a camera, a person, and lighting, none of which exist in this environment — §38's test matrix is untouched. **Also unverified: the panel's CSS**, because the workspace is behind first-run account creation, which I do not do. |
| T022 | Spatial Interaction: seeing your hand, and telling the system about it | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §18 and §19. `components/spatial/HandPreview.tsx` + the calibration flow in `SpatialControl`; `apps/web/tests/spatial-control.test.tsx` grows to 22. **`CalibrationManager` had been dead code for two commits** — computing thresholds nothing consumed — which is the same defect class as D022 with the arrow reversed, so one test asserts the derived thresholds actually persist and are applied to the live session. The preview earns its place through the skeleton rather than the video: a raw feed answers "am I in shot", the landmarks answer "does the tracker agree with what I think my hand is doing", which is the question behind every complaint this feature will ever get. Mirrored, because an unmirrored self-view makes every correction backwards. Drawn from a ref on its own animation frame — **never React state**, since a frame is 21 points arriving 30 times a second and routing that through `useState` would reinstate the per-frame re-render cost T018 removed, in a more expensive form. Calibration refuses rather than returning a number it cannot justify, and the refusal is tested end to end: two poses 0.30 and 0.29 apart produce the actionable message and **persist nothing**, then the researcher is returned to the first pose rather than stranded. **Three real defects found by writing the tests, not by reading the code.** (1) **Frames were gated on a mounted controller**, so the preview and calibration were dead whenever the chart ref had not attached — a researcher could hold a pose perfectly and watch the bar sit at zero, camera on, nothing explaining it. Seeing your own hand must not depend on the chart existing; the observer now gets every frame, and only the *gesture* path needs a controller. (2) **The progress throttle dropped its final update**: rounding to quarters put 11-of-12 and 12-of-12 in the same bucket, so the bar stopped at 92% and the button stayed disabled on a pose that had already been measured. Throttling that drops the last update drops the only one that unblocks anything. (3) In my own test helper, `feed()` restarted its clock at zero for the second pose, so the session's rate governor rejected every frame — the identical mistake the session guards against on restart, made one layer up. Toggles for the preview and the outline (§19); the preview stays mounted when hidden so toggling back does not restart its animation frame. ~~Still unbuilt: sensitivity sliders and the automatic minimise.~~ **Closed by T023.** |
| T023 | Spatial Interaction: settings that take effect, and a way home | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Completes §29's settings and §19's minimise. `apps/web/tests/spatial-control.test.tsx` grows to 27. **The slider range is deliberately narrower than the range storage accepts**, because they answer different questions: `LIMITS` is "what is not insane" and stays wide so a hand-edited or future value survives rather than being silently rewritten, while `SLIDER_RANGE` is "what is worth offering" — a slider spanning 0.2 to 8 would put every usable setting in the first fifth of the track and feel broken while working perfectly. **Sensitivity is asserted at the chart, not at storage.** Persistence is easy to test and proves nothing a researcher cares about; the test drives an identical pinch-and-drag through the whole chain at 0.8 and at 3.5 and compares what the controller was actually told. **Mutation-checked:** removing the `configure` call so settings never reach the running session fails it. That matters because a settings panel whose effect is deferred until restart teaches people it does not work, and they stop touching it. *Reset the view* is a button rather than a gesture, which is the whole point: a researcher whose tracking has gone wrong is exactly the one who cannot gesture their way home (§31). The preview minimises itself once calibration succeeds — it existed to answer "does the tracker see my hand", that question has been answered, and the visualization is what the researcher came for. The toggle brings it back. |
| T024 | Spatial Interaction: a selection the assistant can be told about honestly | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §26, which §49 names as the actual value proposition — not "control graphs with your webcam" but pointing at something and reasoning about it. `throughline_domain/selection.py`, `0033_note_selection.sql`, `journal.ask(selection=…)`; `tests/test_selection.py` (19) + `tests/test_journal.py` (+5) + `tests/test_api.py` (+2). **The mechanism is trivial; what the model is allowed to believe is not.** A selection is points somebody circled on screen — nothing fitted, no test run — and the likeliest wrong outcome is an answer that reads like analysis and rests on a hand movement. Three guards, each mutation-checked: **(1) the wording never calls it a cluster, group, population or sample**, and says outright that nothing was fitted and any difference is unquantified, because a model told "cluster 4" reasons about between-group differences as though clustering had happened; **(2) every statistic is computed here from the coordinates, never accepted** — a mean the interface asserted is indistinguishable, to the model and to the researcher reading the note next week, from one somebody calculated, and `validate` rebuilds the payload from known keys so a supplied summary is unreachable by construction; **(3) labels are untrusted text** — bounded and fenced, since a point named "ignore previous instructions" is a string to report. Deliberately no standard deviation or correlation: either invites the reading that a hand-picked set is a sample of something. **The selection is recorded on the note** (`0033`), because a note saying "these points" with no record of which points is D018 in miniature — a chain legible in the moment and broken a week later. Refusal happens before the model is called, so a malformed selection costs nothing and stores nothing. **A pre-existing bug found while writing the boundary test:** `ask` mapped *every* `JournalError` to 503, so asking about a non-existent object returned **Service Unavailable** — telling the researcher the assistant was down and sending them to check a model configuration that was working perfectly. `NoSuchObject` is now its own type and its own 404; a malformed selection is 400. **Mutation-checked 7/7**, after one mutation had to be rewritten: the first version read a caller summary from the *validated* dict, where the whitelist means it can never appear, so it was inert. Modelling the real mechanism — validate passing the payload through — is caught. **Not wired to the interface**, and the reason is worth stating: the only 3D scatter is the gallery's demo cloud, which belongs to no project and no dataset, so there is nothing a question about it could be recorded against. The UI lands when a project-data 3D view exists. |
| T025 | The corpus in three dimensions, with the loss stated | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | The prerequisite T024 named: a 3D view of *real project data*, so a selection can be recorded against something. `throughline_domain/embedding_space.py` projects passage embeddings (256-d) to three via PCA; `GET /api/projects/{id}/embedding-space`; `tests/test_embedding_space.py` (13) + `tests/test_api.py` (+2). Each point is a passage, so it resolves to a source and a locator — which is what makes a question about a selection answerable. **A projection always looks like structure whether or not it contains any**, so explained variance travels with the coordinates rather than being available on request: three components carrying 60% and three carrying 6% produce visually identical pictures. Refuses rather than draws when it cannot be honest — fewer than 4 points (three points in 256-d lie exactly on a plane, so the picture would show perfect structure that is a property of arithmetic), zero variance (almost always a broken embedding step, and it says so), and **vectors from more than one model**, which the schema comment had anticipated in prose and nothing had enforced. Truncation is reported rather than silent. **Mutation-checked 7/8, and the survivor is recorded rather than hidden.** The orientation pin cannot be failed by any local test: the `ORDER BY` already hands SVD an identical matrix and numpy is deterministic for identical input, so on one machine it is redundant. What it covers is two machines — Accelerate and OpenBLAS can return opposite signs, so the same corpus exported on a Mac and on a Linux server would be mirror images. Unreachable from here, so it is a known gap in the evidence, the way D020 is. **Three of my own errors, each caught by measuring.** (1) I claimed the sign convention keeps the view stable as the corpus changes; it cannot, and a test caught it — when the leading eigenvalues are close the components genuinely *rotate* (measured |cos| 0.5), which is PCA working. The docstring now promises only reproducibility for the same points. (2) The first fixture, `sin(seed + i*0.37)`, spans a **2-dimensional** subspace, so the third component explained 0.000 and pointed in a noise direction — the tests read as covering a 3-D case while exercising a degenerate one. (3) Replacing it with near-random values was the opposite failure: eight points in 256-d have near-equal eigenvalues (0.178/0.163/0.146), so components rotate on any change. The fixture now has separated scales, like a real embedding. numpy is **declared** in `research-domain` rather than borrowed from `throughline-ingestion` — the accident the two lines above it in that file were written about. |
| T026 | The loop closed: point at a passage, ask about it, and the answer is recorded | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §26 end to end, and the first screen where the gesture work stops being an engine. `components/embeddingspace.tsx` + a workspace section; `apps/web/tests/embeddingspace.test.tsx` — 11 tests. Points are passages, so selecting one and asking about it produces a model note recorded against the source's research object. **One backend gap had to close first:** the projection returned `source_id`, but the journal anchors on *research objects*, so a selection had nothing for an answer to attach to — D018's defect, arriving by a new route. Points now carry `object_id`, null when the source has no research object yet, and the interface **says why it cannot ask** rather than greying out a control in silence. **The screen's two obligations are both refusals to flatter the picture.** Explained variance is stated *above* the chart, not below: read underneath, a number is a correction to an impression already formed rather than a frame for it. Below 25% the caption stops reporting a decimal and says what it means — that most of what separates these passages is not visible, and apparent groupings may not survive in the full space — because "9%" is a number most readers cannot convert into "this is close to noise". And it never draws an empty chart: a projection that could not be made shows its reason, since an empty scatter is indistinguishable from a corpus with no structure and a reader takes the second meaning when the truth is the first. **The selection travels as coordinates only**, asserted at the boundary that could break it: a test parses the request body and fails if anything resembling a statistic appears. Every number the model sees is computed server-side from those coordinates. The answer is attributed to the model on screen, never to the researcher. **One test error worth recording:** the first version clicked the canvas centre to select a point and selected nothing — the origin of the *data* is rarely where a point is. It now sweeps, which is robust to the camera, the zoom and the data all changing, and is also what a researcher does. |
| T027 | Asking about a region, without calling it a cluster | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §25, implemented against the tension in it. The section asks for *cluster* selection; this product refuses the word, because nothing was fitted and no test was run — so what it offers is the passages **near where you pointed**, said in those terms on screen and in the prompt. A test asserts the string "cluster" appears nowhere in the rendered page. `selectRegion` on the controller seam, `within()` in `Volume`, a reach control in the embedding view; +6 `volume-controller`, +4 `embeddingspace`. **Deliberately not a new gesture.** §9 and Rule 6 both say a gesture must earn its place, and one that only changed *how many* points the same point-and-pinch gathers would not. Reach is a setting, defaults to zero (one passage), and the mouse honours it identically — a capability reachable by hand and not by pointer would make the camera mandatory for part of the chart, which is Rule 5's failure in different clothes. **Adding the command to `IntentCommand` produced five type errors, one per implementer**, which is the exhaustiveness check in `apply` doing exactly what it was written for: a new command cannot be silently unhandled. **A real bug I introduced, caught by a test rather than by reading:** `within()` put the *normalised* point in `datum` where `nearest()` puts the original. The normalised copy exists to be drawn — coordinates rescaled to a unit cube, none of the caller's own fields — so a region selection would have sent the model **coordinates in the wrong space** and dropped the `object_id` the answer needs to be recorded against. **And a second:** selecting a region set no anchor, so the region appeared with no question box and nothing to do with it. The nearest point is now the anchor, which is also where the note lands. |
| T028 | Runnable, and testable, by somebody with nobody to ask | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `docs/TRY_IT.md` — a walkthrough for exercising the whole product solo, including what each startup failure means and exactly what to judge about the gestures. `tests/test_try_it_guide.py` (13) checks its claims against the code: every script it names exists, every npm command is defined, every button label it tells you to press is on screen, every sidebar item exists, and the startup strings it quotes are the ones printed. A guide that drifts is worse than none, because it is followed while unattended. **Three real bugs, all found by running the thing rather than reading it.** (1) **The port check did not work.** It bound `127.0.0.1` with `SO_REUSEADDR` and reported a free port while a server was plainly serving on it — servers listen on the wildcard address, and a loopback bind with address reuse is allowed alongside one. So `dev` started three processes and then died on an address-in-use traceback, which is precisely the situation the check existed to prevent. It now asks by *connecting*, and the test that would have caught it binds `0.0.0.0` the way a server does — verified by reverting to the old implementation and watching it fail. (2) **The startup banner never appeared under redirection.** Python block-buffers stdout when it is not a terminal, so the one message telling somebody where to go and whether hand tracking is installed sat in a buffer for the lifetime of a process that never exits. Visible in a terminal purely by luck. (3) **My own fix broke startup entirely** — a regex meant for `print(` matched a multi-line `subprocess.run(` and passed it `flush=True`; caught within a minute by running it again, which is the argument for running it again. Also: the hand-tracking model's presence is reported at startup rather than discovered in the browser as a 404 on a `.task` file at the moment somebody switches a camera on. |
| T029 | A page for finding out whether the tracking works here | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `/gesture-check` — no account, no project data, a synthetic cloud and the tracker's own numbers. It exists because every reliability claim in the gesture layer rests on synthetic landmarks, and behind a sign-in the first thing anyone testing tracking must do is remember a password; on a colleague's laptop they must be given an account. `apps/web/tests/gesture-check.test.tsx` (7), session frame-rate reporting +4. Frames-per-second is published about **once a second**, on the tracker's own clock — per frame would be a measurement disturbing what it measures, and wall-clock would answer how fast the test ran. **It also unblocked verification I had never been able to do**, because it is the first page in this product reachable without an account — and looking at it in a browser found two real bugs that no test in this suite could have. **(1) A hydration mismatch on every page carrying `SpatialControl`.** The support check reads `navigator`, which does not exist on the server, so the server rendered nothing and the browser rendered a button — React threw *"server rendered HTML didn't match"* on the workspace, the gallery and the embedding view. Invisible to the suite by construction: happy-dom **has** a `navigator`, so both renders agree in tests and disagree in production. Fixed by rendering nothing until mounted. **(2) The 3D chart was geometrically wrong in any narrow container.** The canvas pinned `width` and `height` in CSS with `maxWidth: 100%`, so a container narrower than the chart scaled the width and left the height alone — the bounding cube drew as a tall trapezoid and every mark sat somewhere it did not belong. A projection wrong by a scale factor is the most convincing kind of wrong, because it still looks like data. Fixed with `aspectRatio` and an automatic height; confirmed by eye at 608px, where the cube is now a cube. happy-dom lays nothing out, so no test here could have seen it. The page states what a bad number means — around 30 frames a second is healthy; many more grabs started than completed means the pinch threshold is wrong for that hand — because a diagnostic nobody can interpret is decoration. |
| T030 | Haptics that are real, and honest about where they can be felt | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `lib/spatial/feedback.ts`, `throughline_domain/haptics.py`, `GET /api/haptics`, `POST /api/haptics/tap`; `tests/test_haptics.py` (7) + `apps/web/tests/feedback.test.ts` (17) + volume detents (4). **The easy version would have been a lie.** `navigator.vibrate` is Android-only and Safari does not implement it, so on a laptop it is a no-op wearing the label "haptics". But the constraint is the browser's, not the machine's, and this product already runs native code beside it — so the local API fires a real macOS trackpad tap. **Verified end to end: all three Apple patterns return `performed: true`.** **The limit decides whether the feature is honest, so it is stated everywhere it could mislead.** The actuator is in the trackpad; a hand held in mid-air has nothing near it and cannot feel the pinch. The capability carries a `felt_where` string rather than a boolean, the panel prints it beside the switch, and the guide says it before anybody judges the feature. Where it genuinely earns its place is the pointer path — Rule 5's co-equal input — which is a hand *on* the actuator. **So the pointer path is now wired**, which the first version argued for and then did not do: a light detent on landing on a point, the sharper pattern on a selection, nothing when a click chose empty space (a tap would confirm a selection that did not happen). Rate-limited at 70ms **in the feedback layer rather than in each caller**, because it is a property of what a person can distinguish, not of any one chart — sweeping a dense scatter crosses dozens of points a second, and unlimited that is a rattle rather than a detent. `onDetent` is an opt-in callback rather than the chart emitting feedback itself: a primitive drawn into a report has no business making a machine tap, and a chart that owned its own policy would be one every host had to remember to silence. One shared `Feedback` for the machine, because one trackpad and one audio graph — two instances would rate-limit independently and the limit would stop meaning anything. Security and latency: the tap takes a JSON body, which forces a CORS preflight — without it any page in any tab could buzz somebody's trackpad — and it is fire-and-forget, since awaiting even a local round trip on the frame a gesture engages puts the confirmation behind the thing it confirms. Sound defaults off; `pyobjc` is declared with a `darwin` marker rather than borrowed. |
| T031 | The keyboard reaches everything the pointer does | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §30 and Rule 5, audited rather than assumed — and the audit found a real lock-out. The 3D chart could **rotate** from a keyboard and nothing else: no zoom, no reset, and **no way to select a point at all**. Selection is what feeds a question to the assistant, so a researcher who cannot use a mouse was excluded from the product's headline capability rather than merely inconvenienced. The model is aim-and-press: there is no cursor in a 3D scene and inventing one would be a second thing to learn, so the target is the centre of the view — rotate a point there, then `Enter`. A crosshair is drawn **only while the chart has focus**, because "the centre" is not a place anybody can see otherwise, and a permanent one would be a meaningless mark on a figure in a report. The aim radius is deliberately generous (60px against the pointer's 28): requiring pixel accuracy from the one input that cannot be precise would make the feature technically present and practically unusable. `+`/`-` zoom, `Home` resets, `Escape` clears. All three share the same clamp the pointer and the controller use — one `zoomBy`, because three callers clamping independently is three chances to disagree about the bounds, and the bounds are what stop the scene being lost (§32). Keys it does not handle are left to the browser, so `Tab` still escapes the chart. The keys are named in the `aria-label`, since a control that is reachable and undiscoverable is reachable in the same sense a door with no handle is a door. Also this round: detents wired on the gallery's P13, which was the one interactive chart without them — and the screen I had told the researcher to test gestures on. And the native capability probe is memoised: **three requests were going out for one immutable fact** (page, panel, and React's development double-invocation), with every new consumer adding another. The promise is cached rather than the result, so components mounting together share one request instead of racing several. |
| T032 | One command that says what is wrong with this installation | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | `python scripts/manage.py doctor` — Python version, virtualenv, Node, the hand-tracking model **and its hash**, both ports, the database and migrations, and haptic hardware. `tests/test_startup_checks.py` grows to 11. Written for somebody testing alone: the alternative, which is what happened before it existed, is meeting each problem one at a time, mid-task, through an error that names a symptom several layers from its cause. **Every failing check carries the command that fixes it** — a diagnosis without a next step is a better-worded complaint. The model check verifies the **hash**, not merely existence: a truncated download sits on disk and fails in the browser with a message about WASM, which points nowhere near the cause. **A bug in my own first version, caught by running it.** It reported *two failures* whenever the stack was running — because the ports were held, which is the normal state and precisely the one somebody is in when they run this. A check that calls the working case a failure is one people learn to ignore, and then it is worth nothing on the day it matters. Each port is now asked whether *Throughline* is answering there; if it is, that is reported as "already serving — nothing to do". The closing line was wrong the same way, telling somebody to start what was plainly already running; it now prints the URLs instead. |
| T033 | The video the tracker reads, wrong twice with the same symptom | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Reported as "the gesture is not working". The strongest candidate, found by reading the element the tracker samples rather than by guessing at thresholds: the `<video>` sat at **`left: -9999`** — laid out, but entirely off-screen. A browser is entitled to stop compositing a video nobody can see, which leaves MediaPipe sampling a stale or blank texture on every frame: **camera light on, zero hands, forever, no error anywhere.** **This is the second time this element has been wrong in exactly this way.** It was `display: none` first, which a browser need not decode at all. Both produce an identical, silent symptom, and neither can be caught by this suite — happy-dom lays nothing out. So it is now guarded by what *can* be asserted: never `display: none`, never a negative offset, still `pointer-events: none`, near-zero opacity and `aria-hidden`. It sits at the origin at 2px, on screen by every definition the browser uses and invisible by every definition the researcher does. **And the page now says what is wrong rather than showing numbers that imply it.** The counters were already there and still required interpretation, which is a diagnostic that has offloaded the diagnosis. One sentence, ordered so the innermost failure is the one reported — otherwise somebody is told to improve their lighting while the real problem is that no frame has been read, and they go and change the lighting. When a pinch is not closing it prints the measured distance *and* the threshold it must beat, because "0.041" means nothing without "and it has to be under 0.035". When the pinch **is** closing and nothing moves, it says that is a bug rather than a setting — the one case where sending somebody to Calibrate would be wrong. |
| T034 | Two bugs that each made the gesture do nothing, silently | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Still reported as not working, so I stopped guessing at thresholds and re-read the pipeline. Two real defects, both of which produce *exactly* the reported symptom — camera on, nothing responds, no error. **(1) A second hand in frame disabled one-handed gestures entirely.** MediaPipe reports a second hand whenever any part of one is visible — resting on the desk, holding a pen, half out of shot — and every such frame went down the two-handed path, where zoom requires **both** pinched. Not both pinched meant the machine set `READY` and returned, doing nothing: pinch-and-rotate silently dead, with no way to tell the reason was a hand you were not using. §17's point is that visibility is not intent, and the code applied that in one direction only. It now falls through to one-hand handling, choosing the pinched hand (nobody pinches by accident — that is the premise of the clutch) or otherwise the nearer one by its own span. Four tests, including that two *open* hands still do nothing, so Rule 2 survives the fix. **(2) I had raised MediaPipe's detection thresholds to 0.6**, reasoning from §31 that low-confidence detections must not act. That conflated two jobs: those thresholds decide whether a hand is **reported at all**, while the machine decides whether to act on one. Raising them does not make the system careful, it makes it blind — in ordinary room lighting the hand is never reported, the diagnostics read zero hands seen, and that is indistinguishable from no hand being present. Back to MediaPipe's 0.5 defaults; §31 is enforced where it belongs and where it is tested. Diagnostics now also carry `bestConfidence`, so "never seen" and "seen and discarded" stop reading the same. Also verified this round by driving the real UI in a browser: the three-step camera flow works and a declined permission produces the correct actionable message. |
| T035 | Rotation never worked, and the seam is why | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **A units mismatch across the controller seam, which made rotation impossible from the first commit.** The machine emitted deltas in *normalised image coordinates* — a deliberate hand movement between two 30 Hz samples is about 0.01 — and `Volume.rotate` multiplies by 0.008 because it is written for a **pointer drag in pixels**. So a movement the researcher could plainly see themselves make arrived as 0.00008 radians: five thousandths of a degree. **Both sides were tested and both passed.** The machine's tests asserted a rotate command was *emitted*; the chart's asserted `rotate()` did not throw. Nobody looked at the number crossing between them, and the contract said only *"radians-equivalent deltas; the adapter decides the mapping"* — that vagueness **was** the defect. The command now carries screen pixels, stated explicitly, matching `hover` and `select`, which already used viewport pixels via `toScreen`. The machine converts using the viewport it already receives every frame, so it stays correct at any chart size. `rotationSensitivity` drops 2.2 → 1.0, because 2.2 was tuned against a quantity that never reached the chart and was therefore never a measurement of anything. Three new tests drive the **real machine into a real chart** and assert the scene moved more than 20px, that the delta is in pixel magnitudes, and that it scales with the viewport. All three fail against the old code. **And the lag, which was a separate fault and also a tuning one.** One Euro's `beta` was 0.03, so the cutoff barely rose with speed and a deliberate movement was smoothed as heavily as a tremor — the scene trailed the hand *by design*, and no amount of extra frame rate would have helped. Raised to 0.4, within the range the authors suggest for a limb rather than a mouse; stillness is unaffected because `minCutoff` governs that. Separately, the session's rate gate sat at the same 1000/33 the tracker uses, so it rejected frames the tracker had deliberately produced whenever one arrived a millisecond early; loosened to 1000/45, since it is a backstop rather than a second opinion. |
| T036 | A second chart where three dimensions are honest | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **P15 — a fitted response surface over two predictors**, plus `lib/charts/scene3d.ts`; `apps/web/tests/surface.test.tsx` (14). Asked for "all graphs in 3D", and that request was declined in one direction and met in another. A bar chart in 3D is *worse* than a bar chart — perspective enlarges near bars, occlusion hides far ones, and reading a value off a rotated axis is measurably less accurate — and nothing is gained, because the data has one dimension and the picture has three. **`z = f(x, y)` is different in kind:** it *is* a 2-manifold in 3-space, so the third dimension is in the data rather than added to it, and the shape a researcher wants (ridge, saddle, plateau, optimum) is exactly what a contour plot destroys and rotation restores. §3 lists mathematical surfaces and fitted responses for this reason and does not list bar charts. It also closes a **claim-versus-reality gap**: P13's own docstring has said "fitted response surfaces over two predictors" since it was written, and it only ever drew points. **Three honesty obligations, each a test:** a fitted surface is a *model*, so the caption says so and the observations are drawn over it and never merged into it; a surface occludes far more completely than a cloud, so hidden observations are counted; and cells with no observation near them are drawn faintly, because the smoothest part of a fit is usually the part with no data under it. The data table holds the observations and **not** the surface — tabulating a fit would invent measurements. **The projection is now shared** (`scene3d`) rather than copied. Two implementations of the same arithmetic is exactly how the rotation units diverged: both sides stayed self-consistent and disagreed with each other. Volume was moved onto it in the same commit, which removed a fourth hand-written copy of the zoom clamp. Gestures, haptics, the keyboard path and region selection all work on the surface for free, through the one seam. **Prototypes 1–6 of §39–§44 are complete**: the interaction loop, smoothing and calibration, two-hand zoom, pointing and hit-testing, point-and-pinch selection wired to the data model, and selections becoming AI context. |
| T037 | Air Ink: the core, where it can be judged without a hand | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | First slice of §137–§235. `lib/ink/{stroke,machine,predict,select}.ts`; `apps/web/tests/ink.test.ts` — 22 tests. Deliberately the parts that can be *evidenced* rather than demonstrated: the two locks, the honesty of prediction, and a circle becoming a set of observations. **Ink takes two deliberate acts, not one** (§139). A stray rotation is undone by rotating back; a stray mark on a colleague's figure is something somebody has to notice and delete — so the tool must be armed *and* the pinch used as pen contact. A pointing finger produces no ink at any time, because pointing is what people do while they talk. A pinch shorter than two frames is refused as a mark: one frame cannot be told from a hand closing on its way somewhere else, and a dot is the most common piece of unwanted ink. **Prediction may change how a line looks and never what it claims** (§148, Rule 4/10). The visible stroke is extended about one frame along the measured velocity — abandoned at a turn sharper than ~40°, because corners are usually the part drawn deliberately; abandoned below a speed floor, because a still hand's velocity is tracker noise and extrapolated noise is a twitching tail; and clamped, so a tracking spike cannot throw the line across the frame. Predicted points are **marked**, and `observedPoints` strips them before any selection or measurement — a region bounded partly by a guess would contain observations the hand never enclosed. **A circle becomes a selection** (§160), resolved through the chart's own hit-testing rather than a second copy of the projection — the same lesson as the rotation units: when drawing and picking use different arithmetic they disagree at the edges. Each observation is counted once however many samples hit it, because the count is the number a researcher quotes. A stroke that is not closed is refused as a region and says so (a line through a scatter is a proposed trend, not a selection), and a region catching nothing says *that* rather than returning silence — an empty selection and an empty cluster are different findings. **Not built:** the renderer, the coordinate mappers beyond screen space, erasing, shape and handwriting recognition, and the ink→AI context path. Recorded rather than implied. |
| T038 | Region selection was approximate, and a count gets quoted | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **A defect in code committed an hour earlier, found by asking where accuracy is merely probable.** `selectWithinStroke` resolved a drawn region by *sampling*: it walked a six-pixel grid inside the loop and asked the chart what was at each step. That is an approximation, and it fails in the worst direction — a mark whose hit radius is small, or which falls between two samples, is **missed**, and the selection comes back short with nothing whatsoever to indicate it. The number that comes back is the number a researcher reads aloud, writes into a paper, and hands to the assistant as "these observations". An approximate selection is not a smaller feature than an exact one; it is a wrong answer delivered confidently, which is the specific failure this project exists to prevent. It also had the grid step hard-coded at a value tuned against nothing. **The fix moves the question to the side that knows the answer.** `withinPolygon(polygon)` joins the controller seam beside `hover` and `select`, and each chart tests *its own marks* against the region — exact by construction, cheaper (one test per observation rather than one per pixel of area), and guaranteed to use the same projection the marks were painted with, so picking cannot drift from drawing. `Volume` and `Surface` implement it against the projected positions they already compute. **Six new tests**: a mark deliberately placed between former grid samples and previously missed; a tight loop that must exclude neighbours; the datum carried through so a region can become AI context; an empty region; and — locating each mark by the *centroid* of its catchment rather than the first hover hit — that a region drawn after a rotation still finds what is inside it, which is the rotation-units failure mode restated for selection. |
| T039 | The FDR correction was wrong in the direction that hides itself | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **`benjamini_hochberg` took `m` from the length of its input, and the input is not the family.** The worker corrects across every *completed* run (`handlers.py:381`) and a run can complete without producing a p-value — a descriptive method, or a result that simply lacks the key. Those `None`s were counted in the multiplier, inflating every q-value in the sweep by `(m + k) / m`. Measured on the failing case: four untested entries among eight doubled every q-value, and at an FDR of 0.05 two of the four real findings flipped from surviving to withheld. **The error is conservative, which is exactly why it survived.** It never produces a spurious discovery; it silently withholds real ones, and the researcher sees a shorter list with nothing to indicate that anything was removed. A q-value is a number people quote, it has one definition, and being wrong in the safe direction is still being wrong. Non-finite p-values got the same treatment: a NaN sorted unpredictably, poisoned the monotonicity walk for everything after it, and then failed `q <= fdr` quietly — the old code returned q = 0.003 where the correct answer was 0.002, again by counting it in the family. **Seven tests, pinned against statsmodels' `multipletests(method="fdr_bh")` rather than against arithmetic this project computed**, because a self-consistent wrong q-value is precisely the failure mode. They cover a 25-test family matched to 1e-12, tied p-values receiving equal q (the input order must not decide which survives), monotonicity, the untested-candidate defect itself, missing and non-finite entries, and a wholly untested family. The ties behaviour was checked rather than assumed — walking down from the largest rank means the first of a tied group reached yields the smallest `p*m/rank` and the rest inherit it, so it was already correct, and is now pinned. |
| T040 | Air Ink becomes something a person can actually use | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | The renderer, the recorder and the page — §143, §146–147 — plus the wiring that makes the existing core reachable by a hand. `lib/ink/recorder.ts`, `components/spatial/InkLayer.tsx`, `app/air-ink/page.tsx`; 35 new tests across `ink-recorder`, `ink-layer` and `spatial-control`. **The recorder writes strokes in viewport pixels, mirrored, before anything can go wrong.** This is T035 applied in advance rather than in hindsight: ink has the same units risk as rotation and a worse consequence, because a stroke in normalised coordinates would still *draw* — a renderer scales whatever it is handed — and `withinPolygon` would then return the wrong observations with no visible symptom. Prediction is computed in normalised units *before* that mapping, so the speed floor and step ceiling keep meaning fractions of the camera frame rather than something different on every display. **Two canvases, not one** (§146–147). Repainting every stroke each frame is correct and gets slower the longer somebody draws, so the line lags more at minute ten than at minute one — degradation people blame on their machine rather than report. Finished strokes sit on a layer touched only when that set changes; the open stroke has its own. Two tests hold the line: one asserts a single stroke is painted per frame with twenty already on the canvas, the other counts React commits during a stroke and requires zero. **Two bugs found by mutation testing, not by writing tests that pass.** The prediction-history reset survived deletion because `commit()` cleared it too, so the common path never reached the guard and `clear()` skipped both — consolidated to one reset at the start of a stroke, which is the only place the meaning holds. Chasing that revealed the real defect: `clear()` mid-stroke left the machine in DRAWING with nothing to draw into, so every later frame reported `drawing: true`, `extend` found no stroke, and ink was silently dead until the researcher happened to release the pinch — after clearing a canvas, the natural thing to do is keep drawing. `InkStateMachine.cancelStroke()` fixes it. Eleven mutants killed across the recorder, the layer and the wiring. **A coordinate drift caught in a browser rather than a test.** The ink measures the element it sits in and the chart reasons in its own logical pixels; a 1px border under `box-sizing: border-box` made those 718×518 against 720×520. Measured live, moved the border to an outer element, and re-measured: 720×520, zero offset. happy-dom lays nothing out, so no test in this suite could have found it — the same blind spot that hid the hydration mismatch and the hidden video element. **Frames come from the one tracker.** `SpatialControl` gained `onFrame`, read through a ref because the session captures its observer once at camera start — a directly-read prop would freeze that closure, and a host that re-rendered would feed frames into a stale one for ever. Both the never-called and stale-closure mutants are killed. A second tracker would have meant two inferences per frame and two readings of one hand, putting the pen a few pixels from the pointer on the same screen. **`/air-ink` and `docs/TRY_IT.md` §3b** state the five things that remain genuinely unknown — whether the line feels attached, whether stray dots appear, whether loops close, whether the count matches a hand count, whether it stays fast — because every threshold in this subsystem came from reasoning rather than from a hand, and none of them can be settled by a test. **Still not built:** erasing (§175–178), coordinate spaces beyond screen (object, data, world, surface), shape and handwriting recognition (§166–169), and the ink→AI context path (§198). |
| T041 | A granular pass over the whole thing, and what it turned up | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked to check everything in a granular manner rather than build something new. **Green across the board**: 1116 backend and 523 web tests, 6 skips all carrying the allowlisted `no Neo4j configured` reason, `tsc` clean, eslint at **0 errors and 0 warnings** (from 7), all four routes returning 200 with clean consoles, the API healthy, `doctor` reporting every check ok, and a production build succeeding with all four routes prerendered. The one remaining warning anywhere is a `StarletteDeprecationWarning` from FastAPI's own `testclient`, not this codebase. **The lint warnings were read rather than suppressed, and one was a real defect.** `Binned`'s colour bar took the cells' `colour` closure and never used it — which read as a legend that had forgotten its own palette. It is in fact correct: the bar is painted in *ramp space* while the tick labels are placed through the same `transform` the cells use, so a value's label sits exactly at the ramp position its cell is painted with under any of the three scales. The dead parameter was removed and the reasoning written down, because the next person to notice it would have 'fixed' a correct legend. `Surface`'s unused `project` import was checked for the worse possibility — a second copy of the projection, which is how the rotation units diverged — and it is not: `Surface` goes through `toCanvas` everywhere. `KnowledgeGraph`'s `themeTick` dependency is genuinely redundant with `dirtyRef`, but **nothing in the suite covers the theme repaint**, so removing it would have been an unverified change to working code; the intent is recorded in a targeted disable instead. **Two gaps closed.** `doctor` and `TRY_IT.md` now both point at `/air-ink`, and three tests keep every localhost path either document names in sync with the pages that actually exist — a route that 404s leaves somebody testing alone unable to tell a missing page from a broken install. The route matcher was checked against real data rather than trusted to pass, which caught it rendering the root page as `/.`. **`distDir` is now overridable by environment.** `next build` against the `.next` a dev server is reading corrupted it once and turned every page into a 500 — a symptom that looks like a broken application and has nothing to do with the code. `NEXT_DIST_DIR=.next-check npm run build` verifies a build without touching what is being served; used here, and the running stack was confirmed still serving all four routes afterwards. Next's habit of writing the chosen directory into `tsconfig.json` is documented as the one thing to discard. |
| T042 | Air Ink was unwritable, and the fix was the opposite of the obvious one | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **The first real-hand report: the line moved too fast to write with.** Two causes, both mine. The pen was using `DEFAULT_ONE_EURO` — the gesture layer's tuning, whose `beta` I raised to 0.4 to stop the *scene* trailing the hand during rotation. Rotation is a gross movement where lag is the complaint; writing is a fine movement where steadiness is. And the ink path had **no spike rejection and no dead zone at all**, though §153 names both and the gesture layer has had spike rejection since T035. **The obvious fix was wrong, and measuring is what caught it.** Reading §154 — "when moving slowly, more stabilization" — I first lowered the filter's cutoff hard. Measured against a 60px letter drawn by a hand with tremor, that made it *worse*: the unfiltered hand has an RMS shape error of 1.6px, and `minCutoff: 0.3` turns it into **11.4px**. A low cutoff is lag, and lag on a shape drawn in a second is a phase shift that reads as the shape being drawn wrongly. Across the whole sweep every smoothing setting cost shape accuracy and none bought any back — §153's "avoid over-smoothing" as a number rather than a caution. **What removes tremor is a very small dead zone**, which costs no lag: a still hand moves under a millimetre between frames, a writing hand moves several times that, so a threshold between them separates the two almost perfectly. My first dead zone was 0.004–0.006, five to eight times too large — at that size only 56% of a letter survived. At 0.0015 it removes the tremor and moves the letter's shape error by 0.05px. **Result, measured both ways rather than asserted:** still-hand drift 75.1px → 3.8px (20x steadier) *and* letter shape error 6.19px → 3.52px (43% more accurate). The raw tracker drifts 625px. **Gain**, because a fingertip cannot be placed to the millimetre: below 1 it compresses travel measured from where the stroke began, so the line starts exactly under the finger and only later distance is scaled — reach is kept by re-pinching, the way lifting a mouse works. **The record follows the pen, not the raw landmark**, and that is an accuracy fix rather than a cosmetic one: once gain is applied the ink is no longer the raw fingertip path, and recording the raw landmark would resolve a drawn loop against a different, larger loop than the one on screen — the selection would silently disagree with the picture. Three levels are exposed on `/air-ink` and in `TRY_IT.md`, because the right amount depends on a hand, a camera and a room, none of which the code can see. 15 tests, including one pinning the counter-intuitive finding and one that stops a future reader from "fixing" the handwriting level by lowering its cutoff. |
| T043 | The 3D scene never fit on its own canvas | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **Reported as "I am not able to do anything with this graph", with a screenshot of the fitted surface torn into a fan of slivers.** Not a gesture fault and not user error: the scene was clipped by construction, at the default camera, with no zoom applied. Two faults in `scene3d.ts`, the projection every 3D chart shares. **Perspective was violent.** `FOCAL = 2.6` against a unit cube whose corner sits 1.73 units out left the divisor at 0.87, enlarging a near corner 2.98x while shrinking a far one to 0.60x — a **4.96x swing inside one scene**. A point cloud survives that and reads it as depth, which is why the 3D scatter looked fine for months; a mesh does not, because its near cells balloon past the frame while its far cells collapse. `DEPTH_RANGE` gave no protection: it damps perspective when *sizing a mark* and nothing damped the projected positions, which is exactly why the observations in that figure looked reasonable while the surface under them came apart. **And the frame never fit the cube.** `toCanvas` allowed 0.30 of the half-canvas per scene unit, which accommodates a radius of 1.67 — but a unit cube at an arbitrary rotation has a projected radius of at least √3 ≈ 1.73 before any perspective at all. Every 3D scene here had its corners outside the canvas from the moment it loaded. `FOCAL` is now 6 (swing 1.81x, still plainly a depth cue) and the fit factor 0.26, derived by sweeping the cube over the full yaw range and the clamped pitch range rather than chosen. **`tests/scene3d.test.ts` — 12 tests, and the shared projection had none before.** Both charts tested what they drew; neither tested whether it fit, because that property belongs to the projection rather than to either chart's data. The sweep is recomputed in the test, so changing one constant without the other fails: the old `FOCAL` breaks 3 tests and the old fit factor breaks 2, independently. **A visible way back.** `Home` reset the view and nothing on screen said so, which for anybody using a pointer is the same as no recovery at all — the other half of the report. Both charts now carry a *Reset the view* control, tested by asserting a pixel resolves to the same mark after rotating, zooming and pressing it. The token guard caught two invented CSS variables in the new styles before they shipped. **Not verified visually**: the browser screenshot tool returns black in this environment, so the geometry is evidenced by test and by DOM inspection, and the figure itself still wants a human eye. |
| T044 | The pinch was moving the pen, and no filter could have fixed it | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | **Second real-hand report: Air Ink still unstable, for highlighting on a chart.** Measuring against the actual task said the filters were not the problem — on a 144px highlighting loop every preset already sat at ~2px of shape error, and heavier smoothing made it *worse* (4.1px). So the instability was somewhere else, and it was: **the pen was tracking the index fingertip, and pinching is a movement of the index fingertip.** Closing thumb and index from an open hand travels the tip about half the pinch distance — **65 pixels** on a 720-wide canvas. Every stroke was displaced at pen-down, displaced back at pen-up, and pulled around throughout by unconscious variation in how hard the pinch was held. None of it is noise, which is why the whole of T042's tuning never touched it: **a systematic error was being treated as a random one.** The pen is now the midpoint of thumb and index tips, which is very nearly invariant under pinching — the fingers converge *on it* — while still tracking the hand as a whole, and is also where a pen would physically be held, so it is where the mark is expected rather than merely where it measures well. **The first three tests for this were worthless and the mutation caught it**: they computed the midpoint and compared it with the midpoint, so reverting the pen to the fingertip left them all passing. Rewritten to drive the real recorder with a hand that holds position while its pinch tightens and loosens mid-stroke — the mutant now dies. A second test bug surfaced on the way: the "still follows the hand" case moved 0.10 of the frame per step, exactly the spike threshold, so every frame was correctly rejected as a glitch and the test failed for the right reason. **`tests/chart-conformance.test.tsx` — 24 tests, both spatial charts, one contract.** `Volume` and `Surface` each had their own suite written at a different time against its own fixture, so they could drift apart while both stayed green — the hazard `scene3d`'s own docstring names. Now every assertion runs against both: the complete seam listed rather than sampled, rotation moving a mark by more than 20px, zoom clamped at both ends, `resetView` returning to the exact starting pixel, exact region resolution, and picking still agreeing with painting after the scene has moved. Writing it found that the seam is `resetView`/`deselect`/`pan`/`focus`/`viewport` — three of which no test had ever called on either chart. **The other twelve charts are 2D and stay that way** (§10, DO NOT FORCE 3D). |
| T045 | The specification stops depending on anybody remembering it | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | "Remember each and every line" is a thing I cannot honestly promise — this conversation has been compacted twice while building against the document — so the promise is replaced with a mechanism. `docs/MASTER_BUILD_PROMPT.md` holds all 236 sections verbatim with its `sha256` pinned; `docs/REQUIREMENTS.md` gives every section a row; `tests/test_requirements_ledger.py` (12 tests) fails the build when the two stop agreeing. **`unreviewed` is the load-bearing status.** Filling all 236 rows with plausible judgements would have taken minutes, and every wrong one would be a false claim of coverage inside the document whose only purpose is to prevent false claims of coverage. 37 sections are `built`, 3 `partial`, 22 `not-built` — each naming the code and the test that back it — and **174 are `unreviewed`, meaning no claim has been made**. The test caps that count so it can fall and never rise. **Six ways of corrupting the ledger were tried and all six are caught**: dropping a section, inventing a status, naming a file that no longer exists, claiming `built` with no test, letting the summary table drift from the rows, and a `not-built` row still pointing at code. A seventh guard is the one that matters most — editing the specification fails the build until the ledger is revisited, because an edited specification is precisely when a requirement disappears without anybody noticing. Verified by inserting a line into the spec and watching it fail. The extraction bug is also pinned: the body is checked for leaked `<w:` markup, scoped to the body because the header quotes the offending pattern while explaining it and a whole-file scan reported the explanation as the disease. |
| T046 | Reviewing the ledger, and the gap it found | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | First pass at turning `unreviewed` rows into assessed ones: §36–§47 and §85 read against the code, **174 → 161**. Most came back `partial`, which is the point of having the status — ingestion reads six formats and not SQL or APIs; profiling reports row counts, types, missing fractions and ranges but not duplicates, categories, candidate identifiers or units; large datasets are sampled and the sample is declared, with no aggregation, tiling or level of detail. `§40` and `§41` and `§44` and `§85` are genuinely built. **The find is §42.** The specification calls undo/redo *mandatory* and gives the reason — "spatial interaction dramatically increases accidental-action risk" — and there is no application-level undo at all. Air Ink can undo a stroke, which is §179 and a different thing. Recorded as `not-built` rather than quietly implemented as a camera-history stack, which would have been theatre: the actions §42 is worried about are deletions, filters and transformations, and those do not exist in the gesture layer yet. **§198 built in the same pass**, because reviewing §36 and §37 showed why they are only `partial` — the assistant is told almost nothing. `lib/ink/context.ts` turns a drawn region into the ask endpoint's selection payload, which the API has accepted since it was written. Nothing is summarised on the way across: the count, the mean and the range are computed in `throughline_domain.selection`, so they are calculated rather than asserted by the interface — a mean sent from the browser would be indistinguishable, to the model and to whoever reads the answer later, from one somebody worked out. A mark whose datum has no usable position is dropped rather than defaulted to zero, because a zero is a position and inventing one puts an observation somewhere nobody saw it. 13 tests. **The 500-point limit now exists in two languages and is pinned across the boundary** by `test_selection.py`, verified by changing one side and watching it fail — the duplication is deliberate, so a researcher who circles half the chart is told by the interface that watched them draw it rather than by a failed request with no circle attached. |
| T047 | Speech and gesture on one clock | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §37, §38, §39 and §199. `lib/voice/{timeline,deixis,intent,source}.ts`; 29 tests. **The hard part is not speech recognition.** Turning audio into words is somebody else's solved problem; deciding *what a word referred to* is the feature. The specification's own diagram is the trap it sets — gesture begins at t0, "these" is spoken at t0+400ms, the gesture closes at t0+900ms. **The word arrives while the circle is still being drawn**, so an implementation that resolves against whatever is selected at that instant binds it to nothing, silently. A referent is therefore an *interval*, and a word falling inside one binds to it however late the gesture finishes. Three ordered rules — a gesture in progress wins, then the most recent completed one, then one about to begin — with the forward window deliberately shorter, because binding forward guesses what somebody is about to do rather than describing what they just did. **Each word resolves at its own timestamp, not the sentence's**, which is the only reason "compare this with this" works: the two words are seconds apart over two gestures, and resolving both at the end of the sentence compares a cluster with itself. **An unresolved reference is refused, never defaulted** — not to the current selection, the last thing touched, or the dataset, each of which produces a fluent answer about something nobody pointed at. **Mutation testing caught that this exact guarantee was untested.** Replacing the refusal with "return the most recent entry" passed all 27 tests, because the only refusal case used a gesture old enough to have been forgotten — the timeline was empty, so every implementation returned null. Two tests added where the entry is present and merely out of reach, which is the only shape that distinguishes a refusal from a fallback; seven mutants now die. **Speech proposes and never performs** (§39): a closed verb set, a described proposal, confirmation before anything happens — because a spoken sentence is ambiguous by nature and has no natural confirmation step, so a misheard word must cost a declined proposal rather than a destroyed analysis. **And the recogniser is behind a seam for a reason that is not testing.** Chrome's `SpeechRecognition` streams microphone audio to Google. It is one line of code and it would break the first claim this project makes about itself — a researcher thinking aloud about unpublished data would be uploading it, with nothing in the interface to say so. Hand tracking was built the hard way for this reason and speech does not get an exception. A remote source must name who receives the audio or it is refused outright, and the default source is one that produces nothing and says why. Typed input is the same path, so the capability is reachable without a microphone at all. **No recogniser is connected yet** — that is the next piece, and it is a choice between a local model and a stated boundary rather than a default. |
| T048 | Closing the loop, and two bugs that only appear when it is closed | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Every piece of speech-plus-gesture was tested and the pieces did not add up to a feature: **the ink layer never told the timeline anything**, so every reference resolved to null while all 29 voice tests passed. `InkLayer` now opens a timeline entry at pen-down — not at commit, because by the time a stroke commits the word has already been said — and `/air-ink` closes it with whatever the loop caught. A command box on the page runs the whole path. **Verified live in a browser**: with nothing drawn it refuses, showing "Nothing was indicated around the time \"these\" was said", which is the behaviour that matters most and the one a fallback would have hidden. **Two bugs, both found by mutation rather than by writing tests.** Deleting the cleanup on `strokeCancelled` changed nothing, and chasing why showed the branch was **unreachable**: `strokeCancelled` is emitted from PEN_DOWN, a state reached *before* `penDown` is emitted, so no entry existed to abandon. The reachable leak had no handling at all — a stroke that begins drawing and then records fewer than two points (tracking lost, or every later frame rejected as a spike) is discarded by `commit`, so `onStroke` never fires and the entry opened at pen-down is never closed. An open entry is deliberately never forgotten, because a hand may rest mid-stroke, so **one such stroke would sit open for the rest of the session and capture every word spoken afterwards** under the "during" rule, binding each to an empty referent while reporting success. **And the console noise turned out to be a real defect.** Chrome refuses `navigator.vibrate` until the document has had a user activation, and refuses it by *logging an error and returning false* rather than by throwing — so the `try`/`catch` around it caught nothing and the page emitted one error per hover detent. That is not tidiness: the diagnostic story for hand tracking is that a researcher opens the console and reports what they see, and a page crying wolf several times a second makes a real error impossible to find. Gated on `userActivation.hasBeenActive`, with absence of the property treated as permission rather than refusal, since the engines lacking it never gated in the first place. |
| T049 | Two clocks, and the lag that was never measured | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked to make speech and gesture *sync*, which turned out to be the right thing to ask. **They were on different clocks, and the whole feature silently did nothing.** Hand frames are stamped with `performance.now()` — about 10,000, milliseconds since page load — and the command box stamped an utterance with `Date.now()`, about 1.76e12. Every word was therefore 55 years after every gesture, no reference could ever bind, and there was no error, no log and nothing on screen that looked wrong. The end-to-end test written an hour earlier passed throughout, because synthetic timestamps are consistent with themselves. `lib/spatial/clock.ts` now owns the one clock, and the argument for it is not precedent: **`Date.now()` can move backwards** — NTP corrections, daylight saving, a laptop waking with a stale clock — and a fusion timeline on a clock that jumps backwards binds a word to a gesture that had not happened yet, roughly once a fortnight, unreproducibly. `ReferenceTimeline` now throws on a cross-clock timestamp naming the fix, rather than resolving nothing for ever. **`tests/clock-discipline.test.ts` failed its own first test**, which is the part worth recording: it scanned the libraries and not `app/`, so the guard written for this bug would have watched this exact bug go past. `app/` is where two subsystems meet and therefore where clocks get mixed. Comments and string literals are excluded, so the error message telling a developer not to use `Date.now()` does not flag itself. **And lag is now a number instead of an assurance** (§149). `LatencyRecorder` reports p50, p95, p99 and the fraction over budget across a bounded window — three numbers rather than one because lag is felt as the moments it stops keeping up, and 49 frames at 20ms with one at 400ms averages to 27ms and feels broken. A negative sample is discarded rather than averaged in, since that is the one direction a latency figure must never be wrong in. Wired into the session after all per-frame work, read on `/gesture-check` at 2Hz, and honest about being a floor: it cannot see camera exposure or the compositor and says so. §149 forbids claiming zero milliseconds and this does not. |
| T050 | Measuring before optimising, and declining one requirement on purpose | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Reviewed the performance block — §50–§53, §149, §218, §219 — **160 → 154** unreviewed. **§53 is the real gap and it is now measured rather than asserted.** Nothing is off the UI thread: inference, the gesture machine and painting all share it, so whatever `detectForVideo` costs is time React and the canvas painter do not have, thirty times a second — and that surfaces as dropped frames *elsewhere on the page*, which is exactly why it cannot be read off the end-to-end figure. `/gesture-check` now reports hand-detection latency separately against an **8ms budget, half a 60Hz frame**, beside the 60ms end-to-end one. The two are budgeted apart so they can disagree: a tracker can sit comfortably inside 60ms end-to-end while eating most of every frame the renderer needed. Migrating inference to a worker is a real change with real risk, and it should be justified by a number rather than by the specification listing workers — so the number comes first. **§51 is declined, not missed.** It asks for interpolation between gesture states. Rendering is already independent of the tracker — rAF behind a dirty flag — but interpolating *adds* latency by definition, since it smooths by drawing a lagged position, and this subsystem has just been tuned in the opposite direction. The One Euro filter already smooths the input; interpolating on top would smooth it twice and trade away the responsiveness that was the whole complaint. Recorded as `partial` with the reasoning, because a future reader will otherwise implement it as an oversight. **§50 likewise**: no WebGL or WebGPU, and `scene3d.ts` has carried the argument since it was written — a scene graph is around 600KB for what is, at this scale, a 4x4 matrix and a sort. §218 came back genuinely `built`: inference runs inside the rAF tick on the freshest frame and the rate governor drops rather than queues, so no backlog can form because nothing is enqueued. |
| T051 | Undo, for the one action that actually destroyed work | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §42, §96 and §179. `lib/ink/history.ts`; 14 tests. The specification calls undo mandatory and gives the reason rather than leaving it as convention — spatial interaction raises the rate at which somebody does something they did not mean, because a pinch is a decision made by a hand that is also being used to gesture while talking, with a camera guessing at the boundary. **T046 recorded this as `not-built` and declined to build a camera-history stack**, on the grounds that rotating is reversible by rotating back and the destructive actions §42 worries about do not exist yet. That was half right: *Clear* exists, discards every annotation at once, and did so permanently. Forty marks made over twenty minutes gone to one misplaced press, with the interface offering nothing. **An undone clear hands back the same stroke objects**, asserted by identity in the tests, not strokes rebuilt from a description — a history that re-derives what it discarded will eventually re-derive it differently, and the researcher gets back something that *resembles* their annotation, which is worse than losing it because they will not notice. Order is preserved for the same reason a restored stroke goes back at its index rather than on the end: it would otherwise paint over marks that were above it. **The controls say what they would do** — "Undo clearing 12 strokes", not "Undo" — since after a few minutes of drawing the bare verb is not a decision anybody can make (§96). A pinch too short to be a mark is deliberately absent from the history, or the first press of undo would appear to do nothing and the real mistake would need two. Five mutants killed, including rebuilding strokes on restore and appending rather than splicing. `undo()` now returns whether anything was taken back rather than the stroke removed, because "the stroke that was removed" is not a meaningful answer to undoing a clear of twelve. **Still `partial`**: movement, filters, transformations and model changes are uncovered, and those actions do not exist in the gesture layer yet. |
| T052 | An annotation that knows it no longer corresponds | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §143 says never to treat every stroke merely as screen pixels, and every stroke here was screen pixels. The consequence was live in shipped behaviour: **circle a cluster, rotate the chart, and the ink stays while the marks move underneath** — a loop that meant "these four" comes to mean nothing, silently, while still looking like a deliberate annotation. **The obvious fix is wrong for this chart.** §143's data-space example is two-dimensional — X = 42–57, Y = 10–18 — and converts cleanly. A screen loop over a *rotatable 3D* scene has no data-space equivalent at all: depth is ambiguous from a single projection, so there is no region of data the researcher can be said to have circled independently of where they were standing. Inventing one would have produced a confident, wrong region — the failure this codebase spends its time removing. Anchoring the loop to the marks it caught and redrawing a hull would violate §174 by changing what they drew. So the annotation **carries the view it was made in** and can say when that is no longer the view. `viewState()` and `restoreViewState()` join the seam, deliberately **opaque** — a map has a centre and a zoom, a timeline has a range, and a seam that assumed three Euler angles would stop being usable the first time a chart was not a 3D scatter. `sameView` scales its tolerance to magnitude, because a zoom of 4 cannot be judged by the same absolute slack as a pitch of 0.02, and treats a missing snapshot as *not* the same view — never drawn is not evidence that nothing moved. `restoreViewState` ignores a snapshot it does not recognise rather than half-applying it, since a partial restore puts the scene somewhere the researcher has never been. The strokes table shows *Still the same view?* and offers to go back. Two mutants killed; the conformance suite grew to 35 and now covers the pair on both charts. **Still `partial`**: object, data, world and surface spaces are unimplemented, and data space is well-posed for the 2D charts — that is the next one worth doing. |
| T053 | Two bugs in yesterday's undo, both in the layer above it | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Undo shipped correct and the things drawing from it were not, which is where this class of bug usually lives. **Changing the stabilisation level destroyed the undo history.** The layer rebuilds the recorder when the level changes and carried the strokes across but not the past, so a fresh `InkHistory` replaced the old one — and a researcher who adjusted a slider silently lost the ability to recover a clear. Nothing about that is visible: undo simply stops being offered, and the obvious reading is that there was nothing to undo. `adoptFrom` now moves strokes and both stacks in one call, so it cannot be half-done; redo moves too, because carrying only the undo stack would leave somebody who had just undone something unable to redo it for a reason they could not connect to the slider they moved. The strokes are carried unchanged and never re-interpreted — re-stabilising a finished mark would change what the researcher drew after the fact (§174). **And undoing a clear brought the strokes back without their rows.** The page appended readings on draw and emptied them on clear, and a list maintained that way cannot follow an operation that reverses either one: the canvas showed three marks and the table showed none. **The first fix was wrong and the second is the interesting one** — filtering the rows down to the strokes that exist follows an undo correctly and loses on redo, because the row is thrown away rather than hidden. Readings are now never discarded; the table renders the ones whose stroke is currently present. Five tests, expressed against the recorder because that is the contract a page has to follow. Separately confirmed the `navigator.vibrate` gate from T048 is working: `hasBeenActive` is false and the console errors are stale buffer entries, unchanged in count across navigations. |
| T054 | The second chart was never connected to anything | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Reported as gestures not working on the second graph, and that is exactly what it was. **`SpatialSession` takes one controller, and `/gesture-check` has two figures.** `surfaceRef` was created, passed to the chart, and given to nothing that produces gestures — the README's own "written by one part of the system and read by none" defect, in the one place where it does not look like a missing wire but like broken tracking. A researcher waving at a chart that does not respond concludes the camera is at fault. `lib/spatial/targeting.ts` decides which figure a frame is addressed to. **Point to choose, then lock until release** (§189): once a pinch closes the target is fixed however far the hand travels and whatever it crosses, because the alternative is a gesture whose meaning changes underneath the person making it — the scene stops responding mid-drag and something else starts turning. The lock is kept even when the hand leaves the figure entirely, which is the whole point of it, and released when the held figure leaves the page so a gesture cannot be stuck holding something unmounted. **A hand over nothing addresses nothing.** Deliberately not "the nearest chart": a hand resting beside the figures, or moving while the researcher talks, would otherwise quietly steer whichever was closest, which is Rule 2's failure and the reason a gesture layer gets switched off for good. **No change to the session at all** — it already resolves its controller *after* publishing the frame to the observer, so choosing in `SpatialControl.onFrame` is decided in time and the resolver just returns it. The lock uses the same pinch hysteresis the machine does, so it holds for exactly as long as the machine considers the hand closed. **Measured on the real page**, which corrected an assumption: bounds are viewport-relative, so with the saddle scrolled into view the cloud sits at y = -753 and is unreachable, and the saddle is. You address what is on screen — correct, non-obvious, and now pinned by three tests so it is not read as the bug this fixes. Three mutants killed. The integration test failed first for a real reason: 400px-wide fixtures with a pointer at x = 500 landed outside both and fell through to the default. |
| T055 | One pen across the page, not one pen per figure | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Air Ink had the same single-figure limit the gesture layer had in T054: `InkLayer` was sized to one chart's box, so it could only be drawn on inside that chart and strokes were recorded in that chart's pixels. A second figure would have needed a second layer, a second recorder and **a second undo history** — so a mark could not cross from one figure to the other and undo would not span them. `fullViewport` makes the layer cover the window, strokes are recorded in viewport coordinates, and the figure a stroke belongs to is whichever the hand was addressing — which §189's lock makes exactly the right question to ask at pen-up, since the target is fixed for the whole stroke. **The conversion is a subtraction, and only because both sides are in the same frame.** A chart hit-tests in its own coordinates, so the polygon is moved through its `bounds()` before it is asked anything. Getting that wrong is silent in the worst available way: the count comes back, it is plausible, and it describes observations from the other chart. Three tests cover it, including one asserting the raw viewport polygon finds *nothing* — so the conversion is shown to be doing work rather than assumed to be. `/air-ink` now carries a saddle beside the cloud, deliberately a different kind of figure: two identical clouds would make a mis-resolution invisible. Measured live — the layer covers 744x757 against a 744x757 window with correct 2x backing. |
| T056 | Erasing, and the sampling trap for the third time | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §175 method A, §176 and §178. `lib/ink/erase.ts`; 18 tests. **The eraser is measured against its path, not against its samples**, and that is the same defect this codebase met in T038's region selection and again in the ink recorder: a hand moving quickly leaves the eraser's centre in a few widely spaced places, and testing only those points leaves untouched flecks wherever the hand outran the sampling — a wipe that visibly removes most of a line and strews the rest. Distance is to the eraser's *segments*. Pinned by its own assertion as well as by the outcome, so the mechanism is tested rather than inferred: a point midway between two samples 300px apart is touched, and is more than five radii from either. **Erasing the middle splits the stroke** (§178) — a researcher rubbing out the centre of a line expects two lines, not the whole line gone and not a line with an invisible hole that still answers selections along its length. Each fragment is a *subsequence* of the points the hand made, never resampled or refitted, because §174 applies to the remains of a stroke as much as to the whole; each carries `derivedFrom` through repeated splits, since a fragment with no ancestry is indistinguishable from something drawn separately. A run of one point is dropped: it cannot be drawn as a line and would strew invisible dots that still answer selections. **Nothing mutates**, so undo hands back the original object rather than a reconstruction — asserted by identity. **§176 is a mode and the lock lives in the recorder**, not on a button: people wave their hands while they talk, and a second entry point must not be able to route around the check. Four mutants killed. The clock-discipline guard caught `erase.ts` stamping `createdAt` from the wall clock and required an explicit justification rather than passing silently — which is what it is for. **Not built**: §177's resistance and haptic while crossing ink, and §175's other three methods. |
| T057 | The eraser reaches a hand, and a bug in how it recorded itself | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | T056 built erasing and wired it to nothing — a feature that is built, tested and unreachable, which is the defect this project names most often. A wipe now takes ink away **as the hand moves** rather than at pen-up, because a wipe whose effect arrives on release gives the researcher nothing to aim with, and the whole pass is **one entry in the history**: a wipe is one thing somebody did, and undoing it a frame at a time would mean fourteen presses to take back one movement. **A real bug, caught by a test that expected the opposite.** The history recorded "erasing across 0 strokes" for a wipe over empty canvas, because `eraseAlong` builds a fresh array every call and the canvas was being reassigned every frame — so the identity check at pen-up saw a change that had not happened. Assigning only when something was actually taken fixes it and removes a pointless array per frame. **The `rub` operation was also simplified while it was still cheap to.** It held the strokes that changed and spliced fragments back at remembered indices; a pass can remove strokes, split one into two and another into three, and reversing that by index is arithmetic with several quiet ways to be wrong. It now holds the whole canvas either side — two lists of references, exactly reversible — because an undo that *nearly* restores a figure is one nobody notices is wrong. Its description still counts what the pass touched rather than the canvas, or "erasing across 12 strokes" would misdescribe a wipe that took one. **§177 in the form that is worth having**: a haptic tick at the moment the eraser meets ink, on the transition only — a tick per frame along a long line is a buzz rather than a boundary, and the moment of contact is the thing that makes a virtual mark feel like something that was there. Three mutants killed. |
| T058 | Reading a mark as a shape, and refusing to when it is not one | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §181 and the half of §174 that had never been exercised. A circle drawn in mid-air is a wobbly polygon and looks careless in a published figure, so offering a clean one is worth doing — what is not worth doing is any version that loses the original. Line, circle, ellipse and rectangle, read **after** the stroke finishes as §181 requires: morphing while the hand is still moving takes the drawing away from the person doing it, who aims, sees it jump, corrects, and sees it jump again. **The failure worth guarding is not failing to recognise a circle — it is recognising everything**, and the first implementation did exactly that. Scoring by *mean* residual, a loop with seven deliberate lobes scored **0.93** and was offered as a circle, because a seventh of the points being 35% out is small once averaged against the six sevenths that fit. That is the precise case §174 exists to protect: a researcher who drew an irregular boundary meant that boundary. Averaging is simply the wrong summary for "does this look like a circle to a person" — nobody looks at the mean deviation, they look at the parts that stick out. Scored on the **90th percentile** instead: sensitive to a systematic departure, still tolerant of a tracker spike, which the maximum would not be. A test asserts the threshold is doing work by showing the same boundary *is* accepted when it is lowered — a threshold nothing ever fails is not a threshold. **Rectangles keep their rotation**, aligned to the drawing rather than the screen: snapping a sketch upright would move the annotation off what it was drawn around. Deliberately geometry rather than a model, so a mistake can be reasoned about rather than only observed. Three mutants killed, including mean-versus-percentile. Two fixture faults found on the way, both of which blamed the fit for the test — measuring from the centre the fixture was built around rather than the fitted one, then averaging a closed path's points without dropping the repeated closing point, which pulls the centroid off by r/n. **Not built**: arrow, polygon and bracket, and nothing is applied yet — the offer has no control. |
| T059 | §181 finished rather than left partial | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked not to leave things partial, so §181 was completed: arrow, polygon and bracket joined line, circle, ellipse and rectangle, and the offer is now reachable and reversible on `/air-ink`. **A closed mark is walked cyclically, and that was a real bug rather than a refinement.** A hand-drawn triangle came back with two corners and was offered as a *circle* — the corner sitting on the seam, where the stroke ends where it began, has no neighbours on one side, and a walk that stops a window short of each end never sees it. Every closed polygon has a corner at its seam roughly a third of the time. The clustering wraps too, or that corner is counted twice and a triangle becomes a quadrilateral. **The mutation for this survived twice, which was the useful part.** Clamping the indices instead of wrapping fabricates a turn at the final point — comparing a real heading against a zero-length step — so a square still came back with four corners, one of them an artefact, and both the triangle and the seam test passed for the wrong reason. The property that separates them is that **a circle has no corners**: on a smooth loop the artefact has nothing to hide behind. **Four near-right corners is a rectangle, not "a 4-sided shape"** — true and useless; the specific name is the informative one and carries a rotation and side lengths with it. An arrow is defined by the *proportion* of barb to shaft, because a line with the small hook a hand leaves when it stops would otherwise be offered as an arrow every time. A bracket requires both ends to leave the spine on the same side, or any mark that turned twice would be one. **Accepting an offer changes the drawn copy and never `originalPoints`** — asserted by identity — and records that the shape was accepted rather than drawn, so a figure can tell the two apart. Four mutants killed. A fixture fault worth noting: a loop of equal radius in *normalised* units is a visibly squashed ellipse on a 720x520 canvas, so the recogniser was right and the test was wrong. |
| T060 | The lasso, finished in one go | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §180, complete: recogniser to control to page, nothing left partial. **A lasso is a question, not an annotation.** Drawing a loop with the pen leaves a mark *and* reports what was inside it; a lasso is drawn to ask "which of these", so the boundary is handed over on the frame it closes and removed in the same breath. Leaving it would turn every selection into a scribble somebody has to clean up. It is also taken out of the undo history, or the first press of undo after a selection would restore a boundary the researcher had already finished with. It *is* drawn while it is being made, because §180 asks for persistent feedback and a boundary you cannot see is one you cannot close accurately. Resolution reuses `selectWithinStroke` and the same conversion into the addressed figure's frame — it is the same question as a drawn region, so it must not be a second implementation of the answer. **`InkTool` lost `highlighter` on the way.** It was declared in the union and implemented nowhere: a member that reads as a capability and is only a plan, which is the same defect as a README claiming a test count that nothing verifies. The type now says what the product does. Three mutants killed, including the one that lets a lasso become a mark. Verified live: three tools on the page, switching correctly, each with its own instruction. |
| T061 | A ruler for a hand that cannot hold one | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §182, complete in one pass. **A hand in mid-air cannot draw a straight line, and smoothing cannot make it able to** — smoothing removes tremor, and the problem is not tremor: an arm rotates about a shoulder while the researcher believes they are moving it sideways, so the line bows. Acceptable in an annotation, wrong in an axis marker or a trend line, and the only recourse was to draw it again and keep whichever attempt bowed least. **Applied while drawing, which is the opposite of §181 and deliberately so.** A shape offer must wait until the stroke is finished, because morphing a mark under a moving hand takes the drawing away from the person doing it. A straightedge is not an interpretation of what somebody drew — it is a tool they picked up *before* they started, and a ruler that only straightened the line after the pen was lifted would be useless. The same distinction decides where the result lands: a shape offer changes the drawn copy and leaves `originalPoints` alone (§174), while a constrained line **is** what was drawn, so it goes into the record — otherwise a lasso or a measurement would resolve against the bowed path the hand took rather than the line on screen. **Magnetic means near, not nearest.** A hard snap-to-nearest would drag every deliberately oblique line onto 45 degrees and the researcher would be fighting the tool; the tolerance is about twelve degrees, wide enough to aim at without precision and narrow enough that thirty degrees is understood as thirty. **The heading waits, and then reaches backwards.** The first samples of a stroke are a hand accelerating from rest and their direction is mostly noise, so locking to them would give a perfectly straight line pointing somewhere nobody intended — worse than bowing, because it looks deliberate. But leaving those points free puts a visible kink in the first few pixels of every line, so once the direction is known they are pulled onto it, once. Found by a test asserting collinearity that failed by 0.06px. Three mutants killed. |
| T062 | Layers, and the one requirement in them that is not organisational | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §201 and §202, both complete. Layers are usually a convenience; one line of §201 is not — **"AI-generated drawings must be distinguishable from researcher-created drawings"** — and that is why this is a module rather than a `visible` flag on a stroke. A figure carrying a *suggested* trend line makes a different claim from one carrying a trend line somebody committed to, and the difference has to survive being screenshotted, printed in greyscale, and read a year later by somebody who was not there. It cannot depend on the reader remembering which layer was on. **So origin is authoritative and enforced at render time, not expressed through style.** A caller can set an assistant stroke's colour, width and dash to match a researcher's exactly — by copying a style, by a future control offering "dash" as an option, or deliberately — and the mark is still drawn dashed, because the renderer reads `origin` rather than trusting what it was handed. The mutation that removes the override kills three tests. Dashed specifically because it survives what a colour or a weight does not: greyscale, a small screenshot, a colour-blind reader. "It looked slightly fainter" is not a distinction anybody can rely on. A researcher's *own* dash is deliberately a different pattern, so choosing one does not make their mark read as the assistant's. **A stroke with no known layer is drawn rather than swallowed** — a mark on a missing group is still a mark somebody made, and hiding it would lose work silently, which is worse than showing it unattributed. Hiding is not erasing, and a test switches a layer off and on to say so. §202 kept to four colours rather than a picker, because it warns against a floating palette for every change. |
| T063 | The hand had no presence on the page | 3 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked to make it immersive, which turned out not to mean adding tools. **Nothing on screen acknowledged the researcher's hand until ink appeared** — so when a pinch failed there was no way to tell whether the camera could not see the hand, could see it and disagreed about what a pinch is, or the hand was not over a figure. Three different faults, one symptom: nothing happened. That is very likely part of what was behind several rounds of "it is not working". §142 states the requirement as a sentence rather than a feature — *the user must never wonder "am I drawing yet"* — and the useful reading of it is **continuous rather than confirmatory**. A cursor that appears when drawing starts confirms success and says nothing about failure. An arc that fills as the fingers close turns "it didn't work" into "I can see I am at two thirds", which somebody can act on without being told what hysteresis is. Measured against the *same* span-relative thresholds the machine uses, because a readout that said "nearly there" while the machine had already acted would teach the researcher to distrust the one thing telling them what is happening. **`contact` and `active` are separate phases**: the fingers have met, and the system has agreed. The gap between them is the two-frame rule, and showing it is what stops somebody concluding that a pinch which *was* registered a moment later did not register at all. The cursor sits at the pinch centroid, not the fingertip — 65px apart (T044) — or it would teach the researcher to aim wrongly. Nothing goes through React; the hand moves thirty times a second. Four mutants killed. **A generic `magnetTarget` was written for §191 and then deleted**: every chart already does nearest-within-a-radius in `nearest`, because only a chart knows where its own marks are, and a second implementation working from a copy of the projection is exactly how picking and painting drift apart. A tested but unused helper is the defect this codebase keeps naming, so it went rather than waiting for a caller. What the cursor owes §191 is the other half — it is drawn where the hand is and never at what it would select, because a pointer that leapt onto the nearest mark would feel possessed. |
| T064 | An immersion audit, and the four silences it found | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked for a granular audit of everything built rather than another feature. Went through the spatial surface asking one question — *where does the illusion of handling your research break* — and checked each answer against the code rather than against impressions. Hover highlighting turned out to be genuinely present (§15). Four things were not, and every one of them was a **silence**: something the system knew and never said. **§189's lock was invisible**, which is the serious one — it is the most important guarantee in the spatial layer, and with two figures on a page a researcher pinches and cannot tell which they have taken until it moves. If it is the wrong one, they find out by turning a figure they did not mean to. The addressed figure now carries a dashed edge and the held one a solid edge: deliberately different pictures rather than the same one at two opacities, because "the one I would grab" and "the one I have" are different facts. Drawn as an edge only — a tint over a figure competes with the data it is pointing at. **The eraser had no size.** A tool whose extent cannot be seen takes more than intended about half the time, and on an eraser that costs an annotation; it now shows its true radius, faintly, and nothing extra when the reach is smaller than the cursor itself, which would be noise rather than information. **A lasso ended in silence** — the boundary vanishes the instant it closes, which is right, but it left the most consequential gesture in the subsystem as the only one with no detent. **Nothing said which layer a mark would land on**, so a researcher who switched and forgot would draw into a group they might then hide, and conclude the ink had vanished rather than that it was somewhere they were not looking. Three mutants killed, including the one that locks onto nothing. |
| T065 | An introduction that watches, and therefore proves | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §97, complete. The specification is unusually clear about what not to build — no thirty-minute tutorial, progressive rather than up-front, a first session of exactly point, pinch, move, release — and silent on the thing that decides whether it is worth anything: **how a step completes**. **Each step advances on evidence from the hand, never on a button.** A slideshow saying "now pinch" with a Next teaches nothing and confirms nothing; a researcher can click through all four without the tracking having seen them once. Waiting until the pinch actually registers makes finishing the sequence *proof* that gestures work on this machine, with this camera, in this light, for this hand — which matters more here than it would elsewhere, because every report of "it's not working" so far has been a symptom shared by three unrelated faults. Somebody stuck on *pinch* while *point* completed knows the camera can see them and the threshold is the problem. That is a bug report instead of a shrug. **A hand that arrives already pinched does not satisfy "point"**, or the sequence would skip the next step without teaching it and tell the researcher they had learnt something they never did. **Losing the hand pauses rather than restarts**: resetting because a hand left the frame for a moment would punish exactly the person this exists for — the one whose camera or lighting is marginal. Three steady frames, for the same reason the pen needs two: one frame of anything is a hand passing through. **Never a gate** (§13). Steps skip individually as well as together, because being held at the one thing your hand is bad at is what makes somebody abandon the feature, and nothing is blocked while it runs. Remembered afterwards, defaulting to *not done* so a preferences blob written before this existed shows the introduction rather than silently skipping it. Three mutants killed. |
| T066 | The cue that was computed and never shown | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §98, complete — and found the same way the audit found the last four: by checking whether something the system knows is ever said. The cursor had been computing `intent` on every frame since T063 and **never drawing it**. Written by one part of the system and read by none, in code two rounds old. **§98 contains a tension worth resolving rather than splitting.** It asks for a cue when somebody points at something and, in the same breath, says not to clutter the interface permanently. A label pinned to the cursor is the version that loses both ways: it follows the hand across the figure, sits on the data being read, and after ten minutes it is furniture nobody sees — so it is simultaneously clutter and not a cue. The resolution is that a cue is worth something at exactly two moments. **When the answer changes**: the hand has arrived somewhere new, or the tool has, and what a pinch would do is a different answer than it was. **When the hand goes still**: somebody holding their hand over something without acting is deliberating, and a hand in motion has already decided. It fades rather than blinking out, because a label that vanishes draws more attention leaving than arriving. The policy is a pure function so it could be argued with in tests rather than watched. Three mutants killed. Two fixture faults, both blaming the implementation for the test: a "moving" hand written as still-since-zero, which reads as maximally still and made every case return 1; and a recording context that answered `measureText` with undefined, so `.width` threw and the label was skipped entirely — the test read as though the feature were missing when it was the double that was. |
| T067 | A lint warning that was a settings-loss bug | 1 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked for production grade with zero errors, so the two remaining lint warnings were read rather than silenced — and one of them was real. **Finishing the introduction reverted every setting changed since the camera was switched on.** `update` merged into the `preferences` its render closed over, and `SpatialControl` builds its frame handler *once*, when the camera starts; that handler writes `onboarded` when the four steps complete. So a researcher who turned the camera on, raised the rotation sensitivity, and then finished pointing and pinching would find the sensitivity back where it started, with nothing connecting the two events — the kind of fault somebody reports as "it keeps forgetting my settings" and nobody can reproduce. Now merged through a ref, so a change is always against what is stored rather than against a snapshot. Two tests: one that the later write keeps the earlier change, and one asserting the *old* behaviour loses it, so the mechanism is pinned rather than the outcome. **A scan for the defect class that has appeared three times** — something computed, or exported, with no reader — turned up three over-exports rather than dead code. Narrowed anyway, and one mattered: `scene3d` states that `RADIANS_PER_PIXEL` is the one place the constant exists, and an export is an invitation to a second place that stays self-consistent and drifts from the projection, which is exactly how rotation was broken from its first commit. `OneEuroFilter` went the same way: every caller wants the point form, and exposing the scalar invites two independently configured filters on x and y that come apart on a diagonal. **Zero errors and zero warnings** across tsc, eslint, 857 web tests, 1129 backend tests, a production build, four routes, and `doctor`. |
| T068 | A ruler whose main feature is a refusal | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | §183 and §184, both complete, plus §25 assessed as already built (a short click on a lazily created `AudioContext`, off by default). §183 draws "42 mm" under a line between two points; §184 says what to do when that number is not available — *never infer it, say so clearly* — and on these charts the second sentence governs. **The axes are scaled independently**, so a centimetre along one is a different amount of a different quantity from a centimetre along another: there is no length of the line joining two observations. Not hard to compute — **it does not exist**. So the difference is reported along each axis in that axis's own units and the single number is refused, said with every measurement rather than once in a footnote, because the person reading it months later is exactly the one without the surrounding conversation. **Measured between observations rather than screen points**, which is the other half: a point on screen over a rotatable scene is a *ray*, since depth is ambiguous from one projection, so "the place I indicated" only has coordinates when it is a mark that exists. **A bug found by clicking rather than by reasoning.** Selecting one mark and selecting it again reported "0 x, 0 y, 0 z" — a confident answer to a question nobody asked, which a researcher glancing at it would read as a finding (two observations that coincide) rather than as a mis-click. Refused now, **by identity rather than by coordinates**, because two *different* observations at the same place is a genuine and interesting zero and refusing that would hide the finding this exists to surface. Two mutants killed. |
| T069 | Two integration failures found by asking where the pieces meet | 2 | done | Claude (Sarthak) | `feat/wave-2-interpretation` | Asked to keep everything in sync and failing nowhere, so the seams were interrogated rather than the modules. Both findings were at joins that every part passed its own tests on either side of. **Erasing reported a stroke as though it had been drawn.** `committed` was doing double duty — "a stroke was added" and "a pass of the eraser was recorded" — so a wipe that took ink handed the *remaining* stroke to the host, which resolved a selection from it and closed a voice-timeline entry with its targets. The researcher would have held a reference to a region they never indicated, and nothing about it would look wrong. Separated into `committed` and `erased`; the layer now repaints on an erase explicitly rather than by accident. The empty-canvas half of this was already safe, because T048's leak fix covers it — worth knowing rather than assuming, and the test that proved it also hid the dangerous half until a second case reached it. **A typed sentence could not reach the gesture it was about.** Words were dated from *submission*, so drawing a loop and taking ten seconds to type put every word outside the backward window: somebody referring to the circle they had just drawn was told nothing was indicated. Dated from the first keystroke now. **The first fix was also wrong and a test caught it**: spreading the words across the typing interval put "these" 4.3s after the gesture, still outside — and the spread models something that cannot happen, because speech words are spread out by a person gesturing *while speaking*, and **nobody can gesture while typing**. Every word of a typed sentence refers to the same moment, so it is now a short utterance dated from when composition began. The consequence is recorded rather than hidden: "compare this with this", one word per cluster, is genuinely unavailable by keyboard and is a thing speech will do that typing cannot. Six mutants killed. |
| T006 | Commit a `.sas7bdat` sample so the last format claim is evidenced | 0 | done | Claude (Fenil) | `feat/wave-1-definition-of-done` | `tests/test_dataset_formats.py` — reads a real SAS-written file, asserts the stored values and the column labels, and checks a truncated file refuses per §104. Fixture provenance in `tests/fixtures/formats/README.md`. |
| T011 | Make the charts interactive: hover emphasis with a real value tooltip, highlighting linked between a chart and its data table, and brush selection | 4 | done | Claude (Fenil) | `feat/chart-interaction` | `apps/web/tests/chart-interaction.test.tsx` — 10 tests, **mutation-checked**: flattening the emphasis fails 3, cutting the table's highlight fails 1. Closes the two interactions `recommend.py` had declared and nothing implemented. Hover, tooltip and linked highlighting on all **five** primitives `figures.tsx` imports — `Cartesian`, `Interval`, `Binned`, `Density` and `Matrix`; brush on `Cartesian` where the x axis is continuous, reporting the count and range **in the reader's units, not pixels**. Native `<title>` removed from the scatter: it was a browser tooltip, ~1s delayed, unpositionable and invisible to touch. Tooltip also appears on keyboard focus. `prefers-reduced-motion`, `prefers-contrast` and `forced-colors` paths included, because emphasis by opacity alone is the first thing to fail in each. **12 of 13 primitives wired.** `Binned` and `Density` shipped in the first pass with the hook declared and a tooltip rendered but **no `markProps` on any mark** — nothing could set `hovered`, so both tooltips were unreachable. Found by checking attachment rather than presence, after the same miscount had already hidden `Matrix` and `Volume`. Two guards now: a behavioural test per figure, and a source check that any file declaring the hook also attaches it — both fail when the defect is reintroduced. `Matrix` was missed on the first pass and added after: it owned a little hover state of its own, so a survey counting handlers filed it as already-interactive when it had no tooltip, no emphasis and no link to its table — a reminder that counting handlers is not the same as checking behaviour. **Caveat: `Volume` is not wired.** It draws to a canvas, so it has no keyed marks to attach to — hover there needs projecting each point into screen space and hit-testing the nearest, which is a different piece of work from wiring a hook. Counting handlers had it at six; all six were `useState` calls (`occluded`, `moved`), not pointer handlers — the same miscount that hid `Matrix`, caught this time before claiming it done. Recorded as D028. |
| T012 | CI runs only when manually dispatched | any | done | Claude (Fenil) | `feat/chart-interaction` | Actions stopped on 2026-08-17: spending limit reached. The repo is **private**, so minutes are metered — Linux 1x, Windows 2x, **macOS 10x**. A full run billed ~82 minutes, 66 of them macOS; 29 runs in a week exhausted the monthly allowance. **`push` and `pull_request` triggers removed entirely**; the only trigger is `workflow_dispatch` — `gh workflow run ci.yml --ref <branch>` or the Actions tab button. **This gives up the guarantee `ci.yml` exists for**: its own header says every defect this project shipped was invisible because nothing ran it off the author's machine. That protection is now a habit rather than a trigger, and habits lapse. The replacing rule, written into the workflow: dispatch before merging to `main` and before publishing; a branch never dispatched has been tested on exactly one machine. **Gotcha: `workflow_dispatch` only appears once the workflow file is on the default branch**, so this must reach `main` before any branch can be dispatched. **Unverified — Actions cannot run until billing resets or the limit is raised.** |
| T013 | Run as much of CI locally as this machine can | any | done | Claude (Fenil) | `feat/local-ci` | `scripts/manage.py preflight --full` now also builds the Docker image and polls `/api/health` inside it — the same two commands as CI's `container` job, which preflight did not cover. That job has the best catch record in the repo: both Dockerfiles in its history were confidently wrong in ways only an actual build revealed. Skips **loudly** when the daemon is down (`sudo service docker start`), because a silent skip would report success while doing nothing. **3 of CI's 5 jobs now run locally** — Linux suite, web, container. **macOS and Windows cannot**, and preflight now says so at the end rather than letting a green run imply full coverage. |
| T070 | Bootstrap that brings its own Python | dist | done | Claude (Fenil) | `feat/dist-bootstrap` | **Both runtimes are fetched now, and the documented install asks for any Python 3.8+ and git.** `scripts/runtimes.py` pins CPython 3.12.14 (python-build-standalone 20260814) and Node 22.23.2 across five platforms, each verified against a SHA-256 committed here — read from the publishers' release manifests rather than fetched alongside the download, because a checksum carried over the same connection as the file authenticates nothing. `manage.py bootstrap` fetches and re-executes under the right interpreter instead of refusing, guarded by a sentinel so it cannot loop. It also detects and replaces a virtualenv built by a *different* interpreter — newly possible with this change, and otherwise a failure much later with a message about a C symbol. `scripts/install.sh` is the `curl \| sh` front door and deliberately carries **no copy of the pins**; a test asserts it holds no checksum and no patch version, because the same list in two places is a defect this repository has already shipped twice. **Stripped, and the difference is measured: 104 MB against 350 MB.** Everything the product needs was checked on the stripped build before pinning it — pip, venv creation, ssl, sqlite3, ctypes, lzma, bz2, zlib, a real install from PyPI, and **pgserver**, whose cp312-only wheels are the reason the version is pinned at all. Node is 204 MB with npm 10.9.8. Fetch 8.5 s and ~21 s; 0.17 s once present. **Driven end to end rather than asserted.** A fresh `git worktree` with no `.venv` bootstrapped using the fetched interpreter: nine packages, 39 migrations against the bundled PostgreSQL, exit 0. `pyvenv.cfg` records `home = …/python-3.12.14/bin`, so it was the fetched interpreter and not the system's. The full suite then ran **inside that venv: 1290 passed, 13 skipped, 0 failed**. At branch HEAD on the dev venv: **1303 passed, 13 skipped, 0 failed**. Web: **1269 passed across 86 files**. **Linux/WSL only — not dispatched.** `tests/test_runtimes.py` (40) and `tests/test_install_script.py` (11), **none touching the network** — downloads run through `file://` URLs so `urlopen`, the streaming hash and the rename all execute. **Five mutants killed**: checksum never compared, archive member names trusted, a directory counted as installed, backslash separators unnormalised, and a rejected download kept on disk. The fourth **survived the first pass and exposed a test passing for the wrong reason** — the traversal cases carried no valid top-level directory, so the layout check raised before the traversal check was ever reached, and both would have kept passing with the guard deleted. Tightened to assert the reason rather than the exception. **Fixed in passing, found by this work:** `dev` chose node by version and then resolved `npm` with `shutil.which`, which searches *this* process's PATH rather than the chosen node's directory — pairing a new node with an old npm on any machine carrying both. `node_exe()` already existed for exactly this and was already used for `npx`; `dev` was the one call site that did not. **Three caveats, all real.** (1) The wrong-version re-exec is unit-tested but **never exercised against a real non-3.12 interpreter** — this machine has only 3.12. (2) The Linux pins are `gnu`, so **musl distributions such as Alpine are not covered**; stated in the module rather than left to be discovered. (3) Node's *download* branch was exercised directly (`python scripts/runtimes.py node`) but **not inside bootstrap**, because the test machine already had Node 24 and the skip branch correctly fired. All three want `gh workflow run ci.yml`, which is blocked behind D030. **Measured for T072:** the runtimes add **314 MB** (110 Python + 204 Node) on top of the 649 MB base — not currently included in T072's ~850-900 MB 'safe path'. Full install here: 662 venv + 110 Python + 204 Node + 812 `apps/web/node_modules` + 76 `.next` ≈ **1.86 GB**, which is where ROADMAP's 1.9 GB came from. The 850-900 figure depends on `output: 'standalone'` pruning node_modules, and `next.config.ts` does not set it yet. |
| T071 | Double-click launchers per platform | dist | done | Claude (Fenil) | `feat/dist-launchers` | **Three doors, one sequence.** `launchers/Throughline.command` (macOS), `launchers/Throughline.bat` (Windows) and `launchers/throughline.sh` (Linux) each set up on first run and start afterwards — and none of them knows how: all three call the new `manage.py start`, which does what ROADMAP describes as one sequence (detect an existing install, otherwise build one, then launch). Three launchers with three inlined copies of that order would be a fourth thing to drift, which is the defect this repository has already shipped twice. `start` checks the virtualenv's **version** rather than its existence, so an install left by a different interpreter is rebuilt — newly possible since T070 — and it does not launch after a failed setup, because starting anyway replaces a setup error with a confusing runtime one. **The window stays open while it installs**, on all three, which is the one thing T071 asked for by name. **Platform details that are not decoration.** The `.bat` is **CRLF and ASCII-only**: cmd.exe mis-parses LF-only batch files into a wrong branch rather than a clean error, and the console runs in an OEM codepage where a UTF-8 em dash is mojibake in front of the user. A new `.gitattributes` pins both, and that was **verified through an actual checkout** rather than assumed — repo blob stores LF, `git worktree add` yields CRLF. It tries `py -3` before `python`, because `python` on a machine with no Python is the Microsoft Store stub, which prints an advert and exits 9009. The Linux `.desktop` entry's `Exec` is **quoted**: unquoted, a clone under `~/My Research/` splits into two arguments and the entry launches nothing, silently. **26 tests** in `tests/test_launchers.py`; both shell launchers are **driven for real** on their no-Python branch, the one path reachable here without starting the whole stack. **Six mutants killed** — batch reverted to LF, non-ASCII in the batch, `start` never installing, `start` launching after a failed install, the desktop entry hiding the terminal, and the Linux launcher forking to find the repo. One of those mutants initially failed to apply and looked like a survivor; re-applied properly, it was killed. Full suite **1328 passed, 13 skipped, 0 failed** locally, plus 13 more tests added after that run. **The `.bat` is unverified and stays that way.** It cannot be executed from WSL, so asserting its shape is not the same as running it — that needs `gh workflow run ci.yml` on the Windows runner, or a person with Windows. Recorded here rather than implied by a green local suite. Linux got a `.desktop` entry instead of the `.AppImage` the row asked for; see D033. |
| T072 | Base install plus feature packs on demand | dist | open | — | — | **Measured 2026-08-24, not estimated: a clean venv with the nine workspace packages, no extras and no test dependencies, is 649 MB.** Breakdown: scipy 139 (incl. `scipy.libs`), pandas 73, PyMuPDF 64, numpy 71, statsmodels 55, matplotlib+fontTools 62, pgserver 34. Three corrections to earlier guesses. **pyarrow (156 MB) is correctly absent** — it was never base, it was in the dev venv because somebody installed extras by hand, so the optional-extras architecture already works. **pgserver is 34 MB, not the ~150 MB I claimed** — the embedded Postgres is cheap. **pandas ships a 37 MB test suite**, 5.7% of the base, for tests nobody runs on a researcher's laptop; stripping test dirs across all packages is worth ~50-60 MB with no code change. Open question worth deciding: matplotlib+fontTools is 62 MB of publication-figure rendering — base, or a 'publication export' pack? A researcher exploring data may never render a PDF. So: safe path (base + Node + `output: 'standalone'`) ~850-900 MB; optimised path (static web, no Node) ~630 MB. **Blocker for the optimised path, found before attempting it:** `next.config.ts` has a `rewrites()` proxying `/api/*` to 127.0.0.1:8080 so the browser calls same-origin and the session cookie works — and **static export does not support rewrites**. The fix is to have FastAPI serve the static bundle, which preserves same-origin by construction, but it is a day's work rather than a config flag. Measuring harness: `python3.12 -m venv --without-pip` + get-pip (this machine hits the unbundled-ensurepip case `manage.py._venv_has_pip()` documents), then `pip install` each package in bootstrap order. |
| T073 | Check-for-updates button | dist | open | — | — | Never automatic. **Backs up before any migration** (`backup.sh` exists), applies forward-only, and leaves the previous version working on failure — the database is the user's only copy of their research. Points at `main` during the build period, at tags afterwards. Tags rather than a release pipeline: `git tag beta-4` is ten seconds and buys a version with a name, which a provenance tool needs in order to answer 'which version produced this result'. |
| T074 | Optional linked account, and the entitlement seam | dist | open | — | — | Local account stays primary and the app stays fully usable without ever reaching a server. Linking is an action in Settings, stored **per local user** rather than per machine (several researchers share a lab machine). Everything ungated during the build period; gating flips **server-side** so the switch needs no release. New features route through `/api/system/capabilities` from the first commit even while every answer is `available` — retrofitting that seam later touches every call site. **The gate never sits in front of opening existing work.** Also updates the gate screen: 'Nothing is uploaded anywhere' stops being true once an account exists. |
| T075 | Extend `audit_log` coverage, and a summary view | dist | open | — | — | Only **8 call sites** today against a table that already records actor, action, object_type and detail. Worth doing on its own terms — a research tool that can show what was actually done, and when, is answering a question a methods section asks, and `ExplorationLedger` already wants it. It is also the expensive half of any future telemetry, obtained as a side effect. **Uploading is deliberately not part of this**: at a scale we can telephone, a conversation beats a dashboard. |

## Discovered

Found while doing something else. Recorded here rather than fixed silently, so
the cost of each is visible before anybody spends it.

| ID | Finding | Severity | Status | Owner | Evidence |
|---|---|---|---|---|---|
| D001 | A validated spec naming the normalised column name failed in the sandbox. Discovery emits exactly that spelling, so **every auto-discovered analysis failed on any file whose headers had spaces or capitals** — i.e. most real research exports. Invisible because every test dataset was already snake_case. | high | done | Claude (Fenil) | `tests/test_human_headers.py` — 7 tests; the discovery test failed before the fix and passes after |
| D002 | `apps/api/.../app.py` defined 55 of its 99 routes more than once — **76 dead definitions**. FastAPI serves the first, so any edit landing in a later copy was a silent no-op. `/api/dataset-versions/{version_id}/density` was defined nine times. Wave 0 found one instance and treated it as a one-off; it was systemic. | high | **verified** | Sarthak | **Already fixed on `origin/main` (`8df65a6`) by Sarthak — do not spend time on it.** Verified independently by Fenil's session on 2026-08-14: the same AST count against `origin/main` gives 101 decorators, 101 distinct, 0 duplicated, 0 dead. My branch still shows 175/99/55/76 because it predates that merge. |
| D003 | `Matrix.tsx` and `SetRegions.tsx` held raw NUL bytes, which made git and grep treat them as binary and silently skip them. **They were not a defect** — the NUL was a deliberate key separator, chosen because it is the one character that cannot appear in a label. Removing it was tried and had to be reverted: it made `"ab"+"c"` collide with `"a"+"bc"` and turned `split(NUL)` into `split("")`. Now written as ``: identical behaviour, plain-ASCII source. | medium | done | Claude (Fenil) | `apps/web/tests/chart-tables.test.tsx` — two tests that fail when the separator is removed. `tsc` and all 79 web tests passed while it *was* removed, which is why the tests exist. |
| D004 | `Cartesian.tsx:137` carried the comment "the figure describes itself, and a table follows" while no table followed, in any of the thirteen primitives. | low | done | Claude (Fenil) | Closed by T004 |
| D005 | `KnowledgeGraph.tsx` claimed "5,000 nodes at 60fps with a quadtree for hit-testing". **Both halves were wrong.** There is no quadtree — hit-testing is a linear scan (which measures 0.43ms and costs nothing, so the index is unnecessary rather than missing). 60fps holds only after the layout settles: one force tick at 5,000 nodes measures ~155ms against a 16.67ms budget, and ~300 ticks are needed, so that graph spends ~46s laying out at ~6fps. `d3-quadtree` is a declared dependency nothing imports. | medium | done | Claude (Fenil) | `apps/web/tests/graph-performance.test.ts`; comment rewritten with the numbers. Real graphs are far smaller and settle in under a second — a ceiling nobody has hit, not a bug being lived with. |
| D009 | **Would have failed CI on the first PR.** `xlwt` is imported by the `.xls` format test (`importorskip(..., reason="writes the .xls fixture")`) but is declared nowhere — `pip show xlwt` reports `Required-by:` empty, and `manage.py` installs only the workspace packages plus pytest and httpx. It is present locally only because Wave 0 installed it by hand. On a fresh checkout that test skips, and CI's allowlist accepts three reasons, none of them this one — so the build fails on a library nothing declares, which is the exact class of defect that CI file was written to catch. Never seen because CI has never run on a Wave 0 or Wave 1 branch: it triggers on push-to-main and pull_request only, and no PR has been opened. | high | done | Claude (Fenil) | `scripts/manage.py` now installs it with the other test-only dependencies. Verified locally by running CI's own allowlist grep against a `-rs` run: 625 passed, 9 skipped, all nine reasons allowlisted. |
| D008 | **Fixed.** The self-hosted fonts introduced layout shift: CLS **0.083** on the landing page over Slow 4G, against **0.000** with the font requests blocked. The shifting element is `div.l-layer`, a decorative `aria-hidden` parallax layer whose `top:-22%/height:144%` geometry tracks its container, so it moves when text reflows. Within Google's "good" (≤0.1) but 83% of the budget, and it was zero before the fonts. | medium | done | Claude (Sarthak) | **Re-measured first, with per-element attribution, and it was worse than recorded: CLS 0.1044 at 1280x900 over Slow 4G — over Google's 0.1 "good" threshold — with 100% of it attributed to one element, `div.l-layer.l-depth-2.l-halo`, against 0.0000 with fonts blocked.** That layer is a decorative `aria-hidden` band 1.44 screens tall whose `top:-22%` / `height:144%` resolve against the hero, so the 107px reflow moved and resized it. **Fixed by sizing the hero's layers from the viewport instead** (`top:-22vh; height:200vh`) — `vh` does not reflow, and `.l-scene` clips overflow so over-covering is free. **CLS 0.1044 → 0.0558**, and `.l-layer` no longer appears as a shift source at all; mobile (390x844) is unchanged at 0.0287, where the hero is exactly one viewport and the layer never shifted. Neither the metric-matched fallback nor `font-display: optional` was needed, so the serif still paints on a first slow visit. **`max(144vh, 144%)` was written first and was wrong**: at 1280x900 the hero is 1115px so `144%` is 1606px, `max()` picks the percentage, and the height tracks the reflow exactly as before — a fallback that reintroduces the dependency is not a fallback. Coverage verified at 390/1024/1280 wide: the band overhangs the hero by 390-658px everywhere. Harness: `scripts/measure-cls.mjs`. **Caveat: the remaining 0.0558 is genuine content reflow** — the lede, CTA row and hero graph moving as the headline rewraps. Removing it needs `font-display: optional` or reserved space, which is a design trade, not a bug. |
| D007 | **Resolved.** Merge hazard between Wave 1 and main: `main` and `feat/wave-1-definition-of-done` both changed `app.py` from base `9b9b63e`: main removed 76 duplicate routes (−2387 lines), Wave 0 added `_binned_cells`/`_dataset_formats` and deleted a dead `/points` copy. A trial merge gives exactly **two conflicts, one hunk each** (`app.py`, `globals.css`) — small, but resolving `app.py` by taking main's side wholesale **silently makes the binned figure unreachable again**, which is the precise defect Wave 0 existed to fix. `hexbin` appears 0 times in main's `app.py` and `"cells"` twice, against 1 and 9 on the Wave 1 branch. | high | done | Claude (Fenil) | `origin/main` (`8df65a6`) merged into the branch. The conflicting `app.py` block turned out to hold **only** duplicate routes, so main's side was correct there and Wave 0's `_binned_cells`/`_dataset_formats` and the `/points` payload survived untouched in auto-merged regions. `globals.css` kept both sides. Verified by route-set comparison against both parents: 101 distinct, 0 duplicates, no route lost from either side, file parses. 625 Python and 100 web tests pass, typecheck clean. |
| D011 | **A displayed number asserting a claim nothing could support.** `graphs.discovery_map` counts `contradictions` and the workspace overview renders it as a **Contradictions** meter. Nothing has ever inserted into that table, so every project has displayed zero since the schema was written — and zero reads as "nothing in your project disagrees" when the truth is "nobody ever checked". This is the inverse of the write-but-never-read defect the last wave kept finding, and worse: a column nobody reads is a feature doing nothing, whereas a count read from a table nobody writes is a false reassurance on the most prominent screen. The detection existed the whole time — `patterns._contradictions` and `consistency.compare_results` both compute it in memory and return. | high | done | Claude (Sarthak) | Closed by T008. Verified by asserting through `graphs.discovery_map` itself rather than the table, so the meter is what the test proves. |
| D012 | **Fixed.** `consistency.py`'s module docstring named **four** checks that run before direction is compared; its code runs **seven** — F2 (different data version) and F3 (different method) also fire before F7. The docstring is not wrong about the four it names, but a reader treating it as the complete order gets the ranking wrong, which is what happened while building T008: the first `CHECK_ORDER` omitted F2 and F3, and would have told a researcher those explanations were never examined when they had in fact been excluded. | low | done | Claude (Sarthak) | Docstring now lists all six pre-contradiction checks and points at `contradictions.CHECK_ORDER`. Guarded by `test_the_check_order_matches_what_consistency_actually_runs`, which parses the outcome codes out of `consistency.py` rather than trusting either list. The fix is a one-line docstring correction; left open rather than folded into T008 so the docstring change is reviewed on its own. |
| D013 | **Fixed. The knowledge graph was missing half its edges.** `research_edges` holds asserted relationships between objects and is read by `graphs.neighbourhood` and `graph_projection`, which explicitly document it as one of "two kinds of edge, one graph". **Nothing anywhere writes it — not production code, not even a test.** So every graph ever drawn has shown lineage edges only, and `graph_projection`'s edge count is structurally short by the whole asserted-relationship half. The same class as D011: a read with no writer, where the empty answer looks like a complete one. | high | done | Claude (Sarthak) | A completed claim test now asserts an edge between the dataset object and the paper object, typed by the verdict family (supports/contradicts/qualifies/inconclusive), carrying the adjudication's confidence and pointing at the evidence row that backs it. Idempotent on the unique key, so re-testing updates rather than accumulating. `tests/test_claim_test.py::test_a_tested_claim_asserts_a_research_edge`, `::test_the_asserted_edge_reaches_the_neighbourhood_query` (asserted through `graphs.knowledge_graph`, the query the view runs, not the table) and `::test_re_testing_updates_the_edge_rather_than_adding_one`. |
| D014 | **Fixed.** `evidence` was read by `findings.py`, `graphs.py` and `objects.py`, and written **only by tests**. A finding's supporting evidence therefore comes back empty in production for every finding, which reads as "nothing supports this" rather than "nothing was ever recorded". | high | done | Claude (Sarthak) | The cause was one layer up: `ClaimPayload` had no `claim_id` field, so Pydantic dropped the id the interface was already sending, and `evidence.claim_id` is NOT NULL. A completed claim test now records evidence with a direction taken from the verdict family. **A refusal records none** — `::test_a_refusal_records_no_evidence`. That test caught a real bug in the first version: `D14 not_yet_analysed` is `Family.UNDETERMINED`, the same family as a genuinely unstable result, so scoring on family alone filed neutral evidence for a pair that had never been analysed. `RunState` is the discriminator and the taxonomy says so — it is "deliberately *not* a `Family`". `::test_the_evidence_summary_a_finding_shows_stops_being_empty` asserts through `findings.evidence_summary`, the number a researcher reads. `strength` is deliberately left null: it means how strongly the evidence bears on the claim, and `verdict.confidence` is confidence in the adjudication, which is a different quantity. |
| D015 | **Fixed.** `locate_claims` inserted into `claims` and never into `located_claims`, so every extraction detail — the constructs named, the design reported, the locator, and which model at which prompt version produced them — was computed and dropped, and `stored_claims` returned empty for every paper ever read. Its docstring's promise that a disagreement between two readings is attributable rather than arguable had nothing behind it. | medium | done | Claude (Sarthak) | Now persisted, and a re-read replaces the previous reading. **`test_a_re_read_replaces_the_previous_reading` had asserted that invariant since the table was added while performing the delete itself, commented "the real function deletes first" — which it did not.** The invariant was documented, asserted and unimplemented at once. `locate_claims` had never been driven by any test (only its no-passages refusal), so the whole extraction body was unexercised; there is now a model stub and three tests over it. |
| D016 | **Fixed.** The literal-statistic guard fired on its own substitution. `add_block` deleted `{{ref:name}}` before searching, so `"q = {{ref:q}}."` collapsed to `"q = ."` — and the pattern's `[\\d.]` accepts a bare period. The single most natural way to write a corrected q-value into a report was refused, with an error accusing the author of stating a result they had not written. Found while writing the §102 end-to-end test, not by any existing test. | medium | done | Claude (Sarthak) | Refs are now replaced with a word rather than deleted (a digit placeholder would make `q = {{ref:q}}` read as `q = 0` and fail for a second wrong reason). `tests/test_communication.py::test_the_natural_way_to_write_a_referenced_statistic_is_allowed` and `::test_a_literal_beside_a_reference_is_still_refused` — mutation-checked in both directions: reverting the substitution fails 2, disabling the guard entirely fails 4. |
| D017 | **Fixed. 37 names defined twice in one scope**, where Python keeps the last and every earlier copy is dead — editing it does nothing. `app.py` held 30 (five copies of `CompareRequest`, two of `LabelDecision`, and the `health` name bound twice), plus `auth.set_password`, `auth.destroy_other_sessions`, `citations._matches`, `claim_test.normalise_design` and `claim_test._record_in_graph`. **Two were duplicated test functions, so `tests/test_claim_test.py` reported more tests than it ran.** This is D002 one level down: that found 76 duplicate *routes* where FastAPI served the first, so an edit landing in a later copy was a silent no-op; here the arrow points the other way. | medium | done | Claude (Sarthak) | All 37 removed; none differed in behaviour, which is why nothing caught them. The two `health` functions were left alone in substance — they serve **different** routes (`/health` and `/api/health`) and the decorators had already registered both before the name was rebound, so both endpoints always worked; the first is renamed `liveness` so the module name matches the route. Verified by comparing the route set before and after: **102 routes both times, none lost, none duplicated.** Guarded by `tests/test_unwritten_tables.py::test_no_name_is_defined_twice_in_one_scope`. |
| D018 | **Fixed. A finding could not be walked back to its evidence by clicking.** Found while measuring T005c. The finding detail screen renders exactly one interactive element — "Preview this as a library note" — and offers no route to the connection behind it, the analysis run that produced it, or the dataset it came from. The connection detail is the same: it prints `⛓ 0 sources · 1 dataset` as text. The product's own description is "the throughline", and the chain is legible in the database and unwalkable in the interface. | high | done | Claude (Sarthak) | **The cause was one unwritten column, not a missing screen.** `findings.object_id` was never set by either caller, and `graphs.evidence_graph` — the query behind "why do we believe this?" — gates its entire analyses-and-connections branch on it, so it returned claims and nothing else for every finding ever made. The panel that would have shown the chain was already built and always received an empty list. A finding created from connections now gets a research object derived from the objects of the analysis runs behind them, which is the same shape `analysis.py` already uses to record a run as `calculated_from` its dataset version — the last link, not a new mechanism. `tests/test_findings.py` +5, **asserted through `evidence_graph` rather than the lineage table**, because checking the edge alone would leave the thing a researcher opens unproven, which is how this went unnoticed. Mutation-checked: dropping the object fails 2, dropping the project scoping fails 3. The step is now clickable through to the analysis (`apps/web/tests/evidencewalk.test.tsx`, 4 tests), and from there the existing screens already reach the dataset and the source. **Caveat: a finding recorded by hand, or one whose connection has no completed run, still has no chain** — recording it anyway beats refusing, since a researcher's result is not contingent on this system's bookkeeping. |
| D019 | **CI was red on the first run it ever had for this branch, in two different ways, and both were tests rather than product code.** `test_no_key_is_unavailable_rather_than_an_error` asserted the note names `ANTHROPIC_API_KEY`, but `_sdk()` checks the import before the key — so on any machine without the optional `[anthropic]` extra the note is about the missing SDK instead. It passed for the author and failed for everyone else. Separately, two sandbox tests imported POSIX-only `resource` at function scope and died on the Windows runner with `ModuleNotFoundError`, which reads as the sandbox being broken on Windows when what is broken is the test's assumption. | medium | done | Claude (Sarthak) | The key test now stubs `sys.modules["anthropic"]` so the import always succeeds and only the key check is exercised; a second test forces the opposite branch with `sys.modules["anthropic"] = None` and asserts the install instructions. **Verified both ways locally** — the SDK is installed here (0.122.0), and the file also passes with it hidden behind a `sitecustomize` stub, which is the CI condition. The two rlimit tests are now `skipif(executor.WINDOWS)` with the reason stated; the marker was checked by forcing it true and watching both skip. |
| D020 | **The Windows equivalent of "an unapplied limit is admitted in the run" is untested.** D019 skipped the two POSIX tests on Windows rather than porting them, because the mechanism genuinely differs — POSIX refuses a `setrlimit` call, Windows applies ceilings through a Job Object. Whether a Job Object limit that cannot be applied produces the same admission in the recorded run is now unknown rather than covered. | medium | open | — | Needs a Windows machine or a CI-only test, and writing one blind against a platform that cannot be run locally is guesswork pushed into CI. Recorded so the skip does not read as coverage. |
| D021 | **`test_sql_references.py` reported a missing table called `no`.** Writing the first `EXTRACT(EPOCH FROM now() - ...)` in the codebase tripped the guard I added earlier this branch: the `FROM` inside `EXTRACT` is not a table reference. The first fix — excluding names followed by `(` — then broke every `INSERT INTO t(col, ...)`, where the parenthesis is a column list. The second fix still failed, because the engine backtracked to the shorter name `no` so the next character was `w` rather than `(`. | low | done | Claude (Sarthak) | `FROM`/`JOIN` and `INTO`/`UPDATE` are now separate patterns, and a `\b` before the lookahead forces the whole name to be consumed before it applies. Verified both ways: a genuinely missing table still fails the guard, and `FROM generate_series(1, 2)` no longer does. **A false positive is the failure mode that gets a guard suppressed rather than fixed** — which is what its own allowlist comment warns about. |
| D022 | **Thirteen domain modules were called by `app.py` and imported by none of them.** Every request to the routes using them raised `NameError` — not a wrong answer, no answer at all — and had done since those routes were written: `patterns`, `notebook`, `synthesis`, `extraction`, `images`, `reconcile`, `claim_test`, `consistency`, `journal`, `specification`, `compare`, `vocabulary`, `graph_projection`. Confirmed at runtime, not just statically: one browsing session produced 64 `name 'patterns' is not defined` and 38 for `notebook`. `interpret.py` had the same defect with `harmonize`. | high | done | Claude (Sarthak) | Found by reading the dev server's log with the workspace open — **not by the suite**, and the reasons are the finding: `compileall`, which CI and preflight both run, compiles an undefined name without complaint, and no test touched any of those routes, so 900 passing tests coexisted with a large part of the API unable to answer. Guarded by `tests/test_imports_resolve.py`, which fails when a file calls a domain module it does not import, and `tests/test_api.py::test_the_patterns_routes_answer_rather_than_raising`. The live route went from 500 to 401 after the fix — reaching the auth check instead of dying at import time. |
| D023 | **The world map keyed three countries as `undefined`.** `world-atlas` carries the ISO numeric code as `feature.id` and three of its 177 features have none — N. Cyprus, Somaliland and Kosovo, because Natural Earth assigns no code to a territory whose sovereignty is disputed. `String(feature.id)` turned all three into the string "undefined", so React warned that children "may be duplicated and/or omitted" — and an omitted child on a map is a country silently missing. | medium | done | Claude (Sarthak) | Keyed by `id` falling back to a `name:`-prefixed label, so a fallback can never collide with a numeric id. Two tests: `apps/web/tests/geographic-keys.test.ts` reads the **real** topology rather than a fixture, because a fixture would have been written from the same wrong assumption that caused the bug; and `apps/web/tests/gallery-keys.test.tsx` renders the whole gallery and fails on any React key warning. Mutation-checked — restoring `String(feature.id)` reproduces the reported error exactly. **Manual browser checks were misleading for an hour**: Turbopack served a stale chunk, so the warning kept appearing after the fix, and an in-page `console.error` patch gave false negatives because React had already captured the original reference. |
| D024 | **The notebook's New note button was dead.** `createNote` called `window.prompt`, which a sandboxed browser refuses outright — "prompt() is not supported" — so the click threw and nothing happened. Even where it works, a prompt blocks the event loop, cannot be styled, and cannot be tested, which is why nothing covered it. | medium | done | Claude (Sarthak) | Replaced with an inline field, not a modal: naming a note is not a decision that warrants taking the screen away, and `ConfirmDialog` exists for the ones that do. Escape cancels, Enter submits, an empty title is refused, and a cancelled title is forgotten rather than pre-filling the next one. `apps/web/tests/notebook-newnote.test.tsx` — 6 tests, mutation-checked: restoring the prompt fails 2, allowing an empty title 1, keeping a cancelled title 1. A repo-wide scan found no other `prompt`/`confirm`/`alert` in the interface. |
| D025 | **A `var(--surface)` that no stylesheet defines**, written by me while fixing D024 — the token is `--panel`. CSS fails silently here: the declaration is dropped, the element keeps what it inherited, and the page still looks nearly right. Nothing in the suite could catch it either, because happy-dom applies no CSS. The same shape as an earlier `className="linkish"` on the exports panel, where no such class exists. | low | done | Claude (Sarthak) | `apps/web/tests/css-tokens.test.ts` fails on any `var(--x)` naming a token nothing defines, across all three stylesheets. It reads tokens set from TSX as well — `--smark-hue` is set per element by `SourceMark.tsx` and is working as designed, and a guard that flagged it would be wrong about the one token doing its job. `var(--x, fallback)` is deliberately allowed. Mutation-checked in both directions: reintroducing the typo fails, adding a fallback does not. |
| D026 | **The figure renderer had D010's bug too.** `visuals.render_visual` wrote to `figures/{visual_id}/{visual_id}.{fmt}` while the row was keyed on (visual_id, format, spec_hash) — so editing a figure and re-rendering produced a second row pointing at the *same* file, and the earlier row's `content_hash` and `bytes` described bytes that were gone. Worse, `stale_renders` exists to say "this file is out of date" and was pointing at the file that had already replaced it. | medium | done | Claude (Sarthak) | Filename now carries the spec hash and the size; `0030_visual_render_size.sql` widens the unique key to include `height_px` (via `COALESCE(..., -1)`, since null is not equal to null in a unique index and two vector renders would otherwise both be allowed). Found while adding sizes, which would have collided a third way — 720px and 1080px were the same filename. `tests/test_visuals.py::test_two_sizes_of_one_figure_are_two_files` and `::test_every_render_row_describes_bytes_that_are_there`, mutation-checked. |
| D027 | **The audit log recorded creation and never destruction.** `delete_project` cascades through sources, datasets, analyses, connections, findings, figures and notes, and wrote nothing at all — no destructive route in `app.py` did. A researcher could erase a year of work and the log that exists to make the work legible would show only the day it was created. | high | done | Claude (Sarthak) | Found while investigating a real workspace on this machine that went from 6 analyses and 6 connections to zero: the audit log held 133 entries, **every one of them `create`**, and 20 `analysis_spec` creations pointed at rows that no longer exist. The gap was not that the answer was unwelcome — there was no answer. Deletion now records what it destroyed, counted **before** the cascade because afterwards there is nothing left to count, with `project_id` deliberately null so the entry does not cascade away with the thing it records. `tests/test_deletion_is_recorded.py` — 5 tests, mutation-checked: removing the audit call fails 5, counting after the cascade fails 1, referencing the project fails 4. The last test reads `app.py` and fails if any route that runs `DELETE FROM` writes no audit entry. **Caveat: this does not recover the lost rows, and cannot** — it means the next such event is answerable. |
| D028 | ~~**Throughline has no 3D visualization**~~ **— I was wrong, and the correction matters more than the original claim.**, which decides the order the Spatial Interaction work can be built in. No Three.js, no WebGL, no Plotly: `KnowledgeGraph.tsx` is canvas **2D** (`getContext("2d")`) with no `z` anywhere, and the thirteen chart primitives are SVG 2D. Every visualization the gesture spec prioritises — 3D scatter, molecular structures, surfaces, engineering models — does not exist here, and §3 explicitly excludes gesture-enabling ordinary 2D charts. | medium | open | — | So camera capture and hand tracking would be enabling technology with nothing to enable, and the spec's own Rule 7 ("scientific usefulness beats visual spectacle") points the same way. The intent layer was built first instead (T014): it is renderer-agnostic by construction, so it is not wasted whichever 3D engine arrives, and it is the only part whose reliability can be proven without a webcam. **The 3D visualization is the prerequisite, not the gesture code.** **Correction (same session):** `components/charts/Volume.tsx` is a complete 3D scatter — primitive **P13**, registered as `renders`, drawn in the gallery — with its own canvas projection, perspective, occlusion counting and drag-to-rotate. I checked for Three.js, WebGL and Plotly and grepped `KnowledgeGraph.tsx`, and never looked at the chart primitives, where the 3D actually was. The conclusion I drew from that gap ("no 3D exists, so gesture work is premature") was wrong; what survives is the narrower and still-true half — Three.js is deliberately absent, because `Volume` reasons that a WebGL scene graph is ~600KB for a 4x4 matrix and a sort, on a product premised on installing locally. **The prerequisite was already met.** T015 wires the gesture layer to it. |
| D029 | **Spike rejection was operating on smoothed coordinates**, which made it dead code for the case it exists to catch. `rotate()` filtered the palm position and then compared *filtered* points against the spike threshold — but One Euro damps a single large jump so thoroughly that the jump never exceeded it. A tracker switching to another person's hand (§31) would have produced a smooth, confident, entirely fictional rotation. | medium | done | Claude (Sarthak) | Found by a mutation, not by review: deleting the guard changed nothing, which is what exposed it. Spikes are now judged on raw landmarks *before* smoothing, and a rejected frame is never fed to the filter — leaving the fiction in the filter's history would make the following frames drift toward a position the hand was never in. `::discards a tracking spike rather than rotating by it`, which asserts the frame is silent rather than merely small. |
| D010 | `render_artifact.render` writes every render of a format to one filename — `storage_root()/artifacts/{artifact_id}/{artifact_id}.{suffix}` — while inserting a **new** `artifact_renders` row each time. So every earlier row's `storage_key` resolves to the newest file. The rows record `byte_size` and `resolved_hash` for bytes that no longer exist, and any route serving a render by id hands back different content than the row describes. Found while building T007, which has to treat superseded rows as history rather than as retrievable copies for exactly this reason. | medium | done | Claude (Sarthak) | Filename now carries the **render id**, so every export is its own file and each row describes bytes that are actually present. `tests/test_communication.py::test_two_renders_of_one_format_are_two_files` and `::test_every_render_row_describes_the_bytes_that_are_actually_there`, both mutation-checked against the old filename. **The second test passed with the bug fully present on the first attempt** — two renders of unchanged content are byte-identical, so the stale row's size matched the file that replaced it; it now edits the document between renders. Cost: renders are no longer overwritten, so exports accumulate on disk. That is the right trade, because `artifact_staleness` exists to answer what a given export said. |
| D006 | **Measured.** Canvas rasterisation was the half D005's numbers explicitly excluded. | low | done | Claude (Sarthak) | DevTools trace over ~304 composited frames while dragging the live graph (`scripts/measure-raster.mjs`): **RasterTask 2 events, 1.1ms total, 0.57ms worst** against a 16.67ms frame budget, with Paint 0.26ms mean and Commit 0.57ms mean / 2.35ms max. **Rasterisation is not a cost here and never was.** The reason generalises further than the sample does: raster work scales with canvas *area*, not node count, so a 794x518 canvas costs the same 0.57ms whether it draws 10 nodes or 5,000 — what scales with nodes is the JS draw and the force ticks, which D005 already measured. **Caveat: the graph measured was the worked example's, 10 nodes and 8 edges**, so this bounds rasterisation and says nothing new about the JS path. The "blocked on Chromium" note was a different machine's problem — Playwright drives the installed browser here with no sudo. |
| D026 | **The workspace's list surfaces are mouse-only.** The finding card in `views.tsx`, and the rows in the sources and connections tables, carry `cursor: pointer` + `onClick` with no `role`, no `tabIndex` and no key handler. There is no tab stop, so a keyboard user cannot open a finding at all — the provenance depth measured in T005c is 2 clicks with a mouse and unreachable without one. Missed by `scripts/measure-provenance.mjs` because it enumerates *interactive* elements, and a `div` with an `onClick` is not one. | high | partly done | Claude (Fenil) | Finding card fixed with an `activatable()` helper and **verified by walking the path on Tab and Enter alone** — focus reaches the card on Tab 8, Enter opens it; before, it could not be reached. A keyboard pass is added to `scripts/measure-provenance.mjs` so the gap cannot reappear unseen. **The two table rows are NOT fixed**: `role="button"` on a `<tr>` trades one broken semantic for another; they need a real control inside the row, which is a change to the table markup. |
| D027 | The T003 worked example's finding carried **no claim, no evidence and no lineage object**, so it had no node in the lineage graph and `/api/objects/{id}/provenance` had nothing to resolve. Distinct from D018/T005c, which is about the *interface* not linking: this is the *data* not being there. | medium | done | Claude (Fenil) | `tests/test_worked_example.py` now asserts the provenance chain resolves, rather than asserting a finding exists. |
| D028 | `Volume` (P13, three-dimensional scatter) has **no pointer interaction of any kind** — it draws to a canvas and nothing hit-tests it, so it is the one primitive of thirteen a reader cannot interrogate. | low | open | — | Needs projecting each point into screen space and finding the nearest within a radius, the way `KnowledgeGraph.tsx` does with its linear scan (measured at 0.43ms for 5,000 nodes, so cost is not the obstacle). Gallery-only today. |
| D029 | `gallery-keys.test.tsx` renders all thirteen primitives at once and ran on vitest's **5s default**, using 2.1s of it on an idle machine. It passed alone and timed out the first time `preflight --full` ran it under load — a flaky test, not a broken one. | medium | done | Claude (Fenil) | Measured both sides rather than assumed: **2.13s at `e06f986` (before the chart interaction work), 2.58s after** — my changes added ~21% and moved it closer to the edge, but 43% of the budget was already gone before I touched it. Explicit 60s budget now, with the reasoning in the file. **Found by `preflight --full`, on its first real run** — the check that exists to catch what a green local suite misses did exactly that. |
| D030 | **CI's skip allowlist never learnt about the speech extra, so a dispatched run would fail the build.** `ci.yml`'s *Every skip must be accounted for* step allows exactly three reasons — Neo4j, `cv2`, and the embedding model — and the suite now skips **four** tests for `openai-whisper is not installed (pip install -e '.[speech]')`. That reason matches none of the three `grep -v` filters, so the step exits 1. Nobody has seen it because CI is dispatch-only (T012) and no run has been fired since the speech tests landed: this is precisely the class of defect the manual trigger trades away. Speech is an optional capability exactly like the other three, so the fix is almost certainly one allowlist line — but `ci.yml` is platform-shaped, so it wants a dispatch to confirm, and that is why it is recorded rather than fixed in passing. | medium | done | Claude (Fenil) | **Fixed.** One line added to `ci.yml`'s allowlist: `openai-whisper is not installed`. It belongs there on the same grounds as the other three — `[speech]` is a declared optional extra pulling in torch, and the extra's own comment in `packages/research-domain/pyproject.toml` already states that a clone which never installs it "still runs the whole suite, with the transcription tests skipped rather than failed". That was always the intent; it simply never reached the list. **Verified by running the step's own filter chain verbatim over real `pytest -q -rs` output** — 13 skips, all accounted for, step passes. Also checked that the guard is not merely weakened: feeding the same input plus a fifth, unknown skip reason still fails the step, and an empty skip list still passes. Branch `fix/ci-skip-allowlist`. **A dispatch is still not green on `main` alone**: the same run also hits D031, whose fix lives on `feat/dist-bootstrap` and is unmerged. Both branches have to land before `gh workflow run ci.yml` reports on the code rather than on the README. |
| D031 | **The README's test counts were stale, and the guard that exists to catch that was already failing on `main`.** It claimed 1252 backend and 1117 web tests against an actual 1302 and 1269, and 6 backend skips against 13. `test_readme_claims.py` asserts the web claim may not sit below the statically countable `it(` blocks — 1117 against 1251 — so **`main` was red before this branch touched anything**, which is worth more than the stale number itself: a failing test on the default branch is a broken smoke alarm. | low | done | Claude (Fenil) | Confirmed pre-existing by stashing the branch and running `tests/test_readme_claims.py` on a clean tree at `8fa5648` — same failure. Numbers replaced with measured ones: `pytest --collect-only` reports 1302; `npx vitest run` reports 1269 across 86 files. Fixed on `feat/dist-bootstrap`. |
| D032 | **Speech is the one optional capability the product never advertises.** `/api/system/capabilities` reports retrieval, analysis, llm and formats — and not speech, although `[speech]` is a declared extra exactly like `[graph]` and `[digitise]`. `ROADMAP.md` says new features route through that endpoint "from the first commit", and this one does not, so the interface cannot know transcription is unavailable until it tries and fails. Worse in the small: `speech.load_model()` does a bare `import whisper`, so the failure is a raw `ModuleNotFoundError` rather than a `SpeechError` naming the extra — the route catches `SpeechError` for a 400 and everything else for a 503, so a researcher without the extra gets `503 Local transcription is unavailable: No module named 'whisper'`. Honest, but it never names the one-line fix, which is precisely the "we cannot read Parquet" versus "Parquet needs one pip install" distinction the roadmap says a capability gate depends on. Found while verifying D030's premise that every allowlisted skip is a capability reported at runtime. | low | open | — | Measured, not read: `load_model()` on this machine raises `ModuleNotFoundError: No module named 'whisper'`, `isinstance(e, SpeechError)` is False, and the message contains neither 'speech' nor 'pip install'. `capabilities()` at `apps/api/.../app.py` returns four keys, none of them speech. |
| D033 | **T071 asked for an `.AppImage` and that is the one door on its list which cannot be a script in this repository.** An AppImage is a squashfs image built by `appimagetool` around a bundled runtime — a build artifact produced by a pipeline, not the "~20 lines" the row estimated. What the request actually wants is *a thing you double-click*, and on Linux that is a `.desktop` entry: GNOME has not run a double-clicked shell script by default for years, so shipping only `throughline.sh` would have satisfied the letter and not the need. T071 therefore ships `manage.py desktop-entry`, which writes one pointing at the checkout. Recorded rather than silently substituted, because an estimate of ~20 lines against a build pipeline is the kind of gap that makes later estimates untrustworthy. **A real AppImage remains undone** and is its own piece of work: it needs appimagetool in CI, a bundled runtime, and a decision about whether the 1.9 GB install belongs inside an image at all. | low | open | — | The substitution is visible in `manage.py.desktop_entry()`'s docstring and in README's Quick start. Verified working on this machine against a scratch HOME: the entry is written, carries `Terminal=true` and an absolute quoted `Exec`. `desktop-file-validate` is not installed here, so it has not been checked against the freedesktop validator. |

## Verified

Move rows here once somebody other than the author has confirmed them. Keep the
evidence; drop nothing.

| ID | Task | Verified by | How |
|---|---|---|---|
| — | — | — | — |

## Done in earlier waves

Wave 0 is recorded in full in `ROADMAP.md`, including the two estimates that
turned out to be wrong. It is not repeated here.
